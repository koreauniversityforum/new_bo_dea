# 다시 구우면 나오는 것들 — 깃에 넣으면 영원히 따라다닌다
앱/
build/*
!build/*.spec
*.zip
*.exe
*.lnk
newsfeed/out/
newsfeed/__pycache__/
__pycache__/
*.pyc

# 실행 중에 생기는 자취
newsfeed/오류기록.txt
newsfeed/cloudflared/
tunnel_url.txt
tunnel.log

# 사람 정보가 들어가는 파일 (제출자 이름·글). 공개 저장소에 두지 않는다
뉴보대_제출DB.csv

# OS 부스러기
desktop.ini
Thumbs.db
.DS_Store

# 카드 굽기 작업물 — 릴리스로 올라가므로 레포에는 안 남긴다
.cards/
.github/cards/node_modules/
.github/cards/package-lock.json

# 메타 토큰·앱 시크릿이 든 파일 - 공개 저장소에 절대 올리지 않는다 (2026-09-18)
메타_계정.json
깃허브_열쇠.txt
다른 PC용/
앱_새빌드/
