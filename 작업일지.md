# 뉴보대 카드뉴스 메이커 — 작업일지

_기록 시각: 2026-08-08 · 이 파일은 `뉴보대\` 폴더에만 두고 배포 ZIP 에는 넣지 않는다_

이 문서는 **왜 이렇게 만들었는지**를 남기는 기록이다.
쓰는 법은 `newsfeed\사용설명서.md`, 다음에 이어서 할 일은 바탕화면
`뉴보대 작업 이어서 하기.md` 에 따로 있다.

---

## 1. 무엇을 위한 물건인가

한국대학생포럼(한대포) 산하 인스타 계정 **@news_univ "뉴스 보는 대학생"** 을 굴리는 데
필요한 손일을 줄이려고 만든 프로그램이다. 첫 업로드는 2026-08-03 이었고,
매주 정해진 시각에 카드뉴스를 올려야 하는데 그 준비가 전부 수작업이었다.

한 게시물에 필요한 것이 이만큼이다.

| 무엇 | 원래 하던 일 | 지금 |
|--|--|--|
| 앞장 카드 | 기사 읽고 문구 뽑고 포토샵 | 기사 주소만 넣으면 문구 후보·사진까지 |
| 뒷장(팔로우 유도) | 매번 새로 만들거나 재탕 | 5종 중 골라 문구만 고침 |
| 인스타 캡션 | 처음부터 씀 | 초안 4가지 글투로 뽑음 |
| 무단 퍼가기 | 대책 없음 | 워터마크 세 겹 |

**설계 원칙 세 가지**를 처음부터 지켰다.

1. **설치할 것이 없어야 한다.** 팀원 PC 사정이 제각각이라 pip 설치·계정 발급을
   전제하면 아무도 못 쓴다. → 파이썬 표준 라이브러리만 쓰고, 배포본에는 파이썬을
   통째로 동봉했다. API 키가 필요한 기능(스톡 사진)은 **선택**으로 뺐다.
2. **지어내지 않는다.** 요약은 AI 가 아니라 규칙 기반(문장 순위)이다. 원문에서 뽑아낸
   발췌일 뿐이므로 "우리 계정의 시각" 자리는 **일부러 빈칸**으로 두고, 출처 줄을
   자동으로 붙인다. 없는 사실을 그럴듯하게 쓰는 쪽이 훨씬 위험하다.
3. **내 PC 안에서만 돈다.** 기사를 읽어 오는 것 말고는 아무 데도 자료를 보내지 않는다.

---

## 2. 무엇을 만들었나

소스는 `newsfeed\`, 배포본은 `뉴보대 카드뉴스 메이커.zip` **하나뿐**이다.
(2026-08-07 확정. 예전 브랜드 제거판 `뉴스 카드뉴스 메이커 (공유용).zip` 은 폐기했다.)

### 화면 다섯 개

| 화면 | 주소 | 하는 일 |
|--|--|--|
| 앞장 카드뉴스 | `/` | 기사 → 문구 후보 → 사진 → 1080×1350 카드 |
| 뒷장 만들기 | `/static/outro.html` | 팔로우 유도 마지막 장 5종 + 배경 12종 |
| 피드 글 | `/static/feed.html` | 유사 기사 찾기 + 캡션 초안 4가지 글투 |
| 저장 폴더 정리 | `/static/out.html` | `out\` 에 쌓인 파일 보기·정리 |
| 워터마크 | `/static/mark.html` | 워터마크 설정 + 퍼간 그림 검사 |

### 파일별 역할

```
newsfeed/
  app.py               로컬 서버. 기사추출·요약·유사기사·캡션·저장·폴더정리 API
  extractor.py         기사 주소 → 제목·본문·사진·언론사·발행시각 (readability 축소판)
  summarizer.py        한국어 규칙 기반 요약·제목·후킹 문구
  related.py           같은 시각대 유사 기사 찾기 (구글·빙 뉴스 RSS, 키 불필요)
  feed.py              인스타 캡션 초안 조립 (4가지 글투)
  static/app.js        앞장 캔버스 렌더러 + 편집기
  static/outro.js      뒷장 5종 렌더러 (그리기 전용, DOM 의존 없음)
  static/hiddenmark.js 워터마크 세 겹 + 되살리기·겹쳐읽기
  static/*.html        화면 5개
  assets/              로고 3개 (뉴보대 2 + 한국대학생포럼 1)
  fonts/               Pretendard 5종 (SIL OFL 1.1, 동봉 허용)
```

---

## 3. 왜 그렇게 만들었나 — 판단의 근거

### 배경을 사진이 아니라 코드로 그린다
뒷장 배경 12종은 전부 프로그램이 그때그때 **그리는 그림**이다. 저작권 걱정이 없고,
색+씨앗이 같으면 항상 같은 그림이 나오므로 시리즈물에서 배경을 통일할 수 있다.
앞장이 시사·경제·정치 기사라서 신문 지면·법정 저울·국회 기둥·경제 차트를 앞에 뒀다.

### 검색은 구글 + 빙 둘 다 쓴다
구글 뉴스 RSS 는 한 번에 100건까지 주고 제목·언론사·발행시각이 정확하지만
**링크가 중계 주소라 본문을 못 긁는다.** 빙은 12건뿐이지만 링크에 진짜 기사 주소가
들어 있다. 그래서 둘을 합쳐 쓰고, **자동 검색이 보장하는 범위는 제목·언론사·시각까지**
라고 화면에 명시했다. 본문이 필요하면 주소를 직접 넣는 길(2-1)을 따로 뒀다.

### 캡션에 "우리 계정의 시각"을 안 채운다
`[여기에 우리 계정의 시각을 한두 문장 덧붙이세요]` 는 버그가 아니라 **의도**다.
계정의 관점은 사람이 써야 한다. 프로그램이 채우면 그럴듯한 거짓말이 된다.

### 워터마크를 세 겹으로 나눈 이유
견디는 조건이 서로 달라서다. 하나로는 안 된다.

| | 무엇 | 어디에 | 언제 살아남나 |
|--|--|--|--|
| ① 숨긴 무늬 | 옅은 로고+글자 (1.5%) | 편집 화면 포함 모든 곳 | 인스타 재압축·화면 촬영에도 남음 |
| ② 보이는 무늬 | 크고 진한 로고+글자 (16%) | **내려받은 파일에만** | 지우기 어려움 — 막는 쪽 |
| ③ 파일 속 글자 | 계정·이름·날짜 | 저장한 파일 | 원본 PNG 에서만 |

②를 편집 화면에 안 넣은 이유: 디자인이 가려지면 작업을 못 한다.
①③은 **막는 장치가 아니라 추적하는 장치**다. 막는 쪽은 ② 뿐이라는 걸 화면에도 적었다.

---

## 4. 만들면서 밟은 함정 (다시 밟지 말 것)

### 한국 기사 파싱
- **네이버**는 언론사·날짜가 메타에 없다. 본문의 `data-date-time` 을 읽어야 한다.
- **다음**은 `og:site_name` 이 `"Daum | 연합뉴스"` 이고 날짜가 14자리(`20260807163355`)다.
- 추출된 언론사 이름이 있으면 **호스트 매핑으로 덮지 말 것.** 덮으면 연합뉴스가
  다음뉴스로 바뀐다.
- 검색어는 **제목을 두 번 세어** 뽑는다. 제목 낱말만 쓰면 `명문학교서` 같은 조사 붙은
  토막이 걸려 결과가 0건이 된다.

### 요약기
- `summarizer.summarize()` 는 **최대 4개**, 그것도 서로 겹치는 변형만 준다.
  여러 문단이 필요하면 `rank_sentences()` 를 직접 써야 한다.
- `_headline_from()` 은 **문장 앞부분을 그대로 남긴다.** "제목이 요약문으로 시작하면
  버린다" 같은 규칙을 넣으면 **제목이 전부 빈칸**이 된다. (실제로 한 번 그렇게 만들었다)

### 캔버스·그림
- 어두운 배경에 남색 로고는 안 보인다 → `source-in` 으로 **모양만 남기고 단색으로
  물들여** 그린다.
- 숨긴 무늬를 되살릴 때 `원본 − 흐린판`(고역통과)으로 하면 **사진 잡티·별이 같이 커져서
  글자가 묻힌다.** `blur(1) − blur(4)` 띠통과로 해야 한다.
- 그래도 사진 배경은 안 읽혀서 **겹쳐 읽기**(무늬가 규칙적으로 반복되는 점을 이용해
  여러 칸을 포개 평균)를 넣었다. 이게 결정타였다 — 잡음이 √N 로 줄어든다.
- 무늬 칸 간격은 **글자 폭을 실제로 재서** 정해야 한다. 설정값만 믿으면 계정 이름이
  길어질 때 칸끼리 겹쳐 뭉개져 나중에 못 읽는다.
- 로고 PNG 를 워터마크로 쓰려면 **흰 바탕을 투명으로 빼야 한다.** 안 그러면 네모 판이
  통째로 얹힌다. JPEG 는 가장자리가 번져 있어 딱 잘라내면 지저분하다.
- 워터마크는 **내보낼 때 복사본에만** 심는다. 미리보기 캔버스에 심으면 다음 렌더에서
  지워지거나 두 번 심긴다.
- 로고 이미지는 늦게 도착한다. 다 받은 뒤 **다시 그리라고 알려야**(`onReady`)
  첫 화면에 로고가 나온다.
- 미리보기 캔버스는 `.canvas-box` 로 감싸야 1080×1350 비율이 유지된다.

### 윈도우·배포
- `.bat` / `.vbs` 는 **cp949** 로 저장해야 한글이 안 깨진다.
- `.ico` 는 PNG 내장이 아니라 **DIB 형식**으로 써야 탐색기가 제대로 읽는다.
- ZIP 항목 이름은 반드시 **슬래시(`/`)**. 역슬래시면 탐색기가 폴더로 안 본다.
- `app.py` 가 URL 을 `unquote` 하지 않아 **한글 파일명 asset 이 전부 404** 였다.

---

## 5. 오판한 것 (숨기지 않고 남긴다)

### ① 배포 ZIP 안의 `.bat` 을 확인 없이 덮어써 날렸다
ZIP 안 `카드뉴스 메이커 실행.bat` 은 `newsfeed\` 것과 **이름만 같고 내용이 다른 파일**
이었다(배포본은 루트가 앱 폴더라 `cd /d "%~dp0"`, 그리고 주소 3개 안내).
같은 이름이라 그냥 덮었다가 원본을 잃었고, 재구성해서 넣었다(주소 4개로 보강).

**교훈:** ZIP 갱신 전에 **항목별로 해시 대조부터** 한다. 그 뒤로는 대조 → 다른 것만
갈아끼우는 방식으로 바꿨고, 소스에 없는 배포 전용 파일 4개
(`★ 먼저 읽어주세요.txt` · `뉴보대.ico` · `바탕화면에 바로가기 만들기.vbs` ·
`out/여기에 저장됩니다.txt`)는 아예 손대지 않는다.

### ② 카드 대사 글투에서 제목이 전부 빈칸으로 나왔다
한 문장을 제목과 요약문에 겹쳐 쓰고 "제목이 요약문으로 시작하면 버린다"는 규칙을
넣었더니 `_headline_from()` 특성상 **전부 걸러졌다.** 한 장에 서로 다른 두 문장을
쓰는 구조로 바꿔서 해결했다.

### ③ 되살리기를 고역통과로 만들어 사진 배경에서 실패했다
별하늘·사진 잡티에서 글자가 전혀 안 읽혔다. 띠통과 + 겹쳐 읽기로 바꿔 해결.

### ④ 앱이 멎는 것을 "서버를 두 개 겹쳐 띄워서"라고 진단했다 — **틀렸다**
하나만 띄워도 재현됐다. 진짜 원인은 **검은 창(콘솔) 출력**이었다(§9).
겹쳐 띄운 것은 눈에 띄는 딴 것이었을 뿐인데, 마침 그럴듯해서 거기서 멈췄다.
**교훈:** "그럴듯한 원인"을 찾으면 오히려 한 번 더 갈라 봐야 한다. 한쪽만 바꿔
재현되는지 보는 데 몇 분이면 됐다.

---

## 6. 검증 방법

브라우저 확장이 안 붙어 있어서 다음 방법으로 확인했다.

- **node `vm` + 가짜 캔버스** — `outro.js` 는 DOM 의존이 없어 그대로 돌릴 수 있다.
  5종 렌더·워터마크 좌표·색 조합을 예외 없이 검사했다.
  `hiddenmark.js` 의 파일 속 글자는 **진짜 픽셀 배열**을 물려 왕복 검사했다.
- **헤드리스 크롬** — 임시 `static/_*.html` 을 만들어 그림으로 확인하고 지운다.
  `chrome --headless=new --virtual-time-budget=20000 --screenshot=out.png URL`
- **배포본을 실제로 풀어 동봉 파이썬으로 실행** — 화면·API·거절 동작까지.

마지막 검증 결과(2026-08-07): 서버 쪽 22항목 · 브라우저 쪽 20항목 **전부 통과**,
ZIP 63항목 · 17.5MB · 소스 불일치 0 · 서버 로그 예외 없음.

주요 실측값:
- 직접 주소 본문 일괄 시도 — 4건 중 3건 확보, 0.8초(동시 4개)
- 파일 속 글자 — PNG 에서 정확히 읽힘, JPEG 70% 재저장하면 사라짐(설계대로)
- 숨긴 무늬 — JPEG 70% 로 뭉갠 뒤에도 겹쳐 읽기로 판독
- 숨긴 무늬 진하기 — 1.5% 는 어느 배경에서도 안 보이고, **3% 부터 줄이 보인다**

---

## 7. 지금 상태

| 무엇 | 어디 | 상태 |
|--|--|--|
| 실행 | `뉴보대 카드뉴스 메이커.lnk` (로고 아이콘) | 정상 |
| 배포본 | `뉴보대 카드뉴스 메이커.zip` (17.5MB, 63항목) | 소스와 일치 |
| 소스 | `newsfeed\` | — |
| 설명서 | `newsfeed\사용설명서.md` | 전 기능 반영 |
| 이어하기 | 바탕화면 `뉴보대 작업 이어서 하기.md` | 전 기능 반영 |

**운영상 꼭 기억할 것 두 가지**

1. **보이는 워터마크가 기본으로 켜져 있다.** 우리 계정에 깨끗하게 올리려면
   `워터마크` 화면에서 **저장 직전에 `끄기`**.
2. **원본 PNG 를 따로 보관**해 둘 것. 인스타에 올리면 파일 속 글자가 사라져
   "이게 우리 원본"임을 보이기 어려워진다.

**한국대학생포럼 계정 철자** — `@universityfourm_korea` 다.
`forum` 이 아니라 `fourm` 이며, 사용자가 준 그대로다. 오타처럼 보여도 고치지 말 것.

---

## 8. 2026-08-07 (2차 보완) — 미완 항목 처리

사용자 지시로 아래를 처리했다. **되살리기·삭제는 손대지 않았다**(그대로 두라는 지시).

### ① 제목을 고를 수도, 직접 고칠 수도 있게
피드 글 화면에 `제목 추천` 칸을 붙였다. **눌러서 고르기(칩) + 직접 고치기(입력칸)**
둘 다 되고, `글에 넣기` 로 글의 제목 줄을 바꾼다. 카드 대사 글투면 표지의
`제목 ▸` 줄, 나머지는 맨 첫 줄이 바뀐다.

### ② 언론사 상투·홍보 문구 걸러내기 — `extractor._drop_promo()`
두 갈래로 잡는다.
- **드러나는 말투** `PROMO_LINE` — …조명합니다 / 소개합니다 / 구독 / 제보 / ▶ / AI 학습 금지
- 🔑 **문체가 튀는 줄** — 한국 기사 본문은 '-다' 로 끝나고(했다·밝혔다) 안내문은
  '-습니다' 체다. 본문 대부분이 '-다' 체일 때 혼자 '-습니다' 로 끝나는 **긴 줄**은
  기사 문장이 아니라고 본다.
- 🔴 **문체 규칙은 본문이 '-다' 체일 때만 쓴다.** 방송사 기사처럼 처음부터 존댓말인
  글에 그냥 걸면 본문을 통째로 지운다. 그래서 비율을 먼저 세고 판단하고,
  다 지워졌으면 판단이 틀린 것으로 보고 원래대로 되돌린다.
- `feed._sents()` 에서도 한 번 더 턴다(문장에 붙어 들어온 경우).

### ③ 앞장 오른쪽 패널에 워터마크 켜기/끄기
`보이는 워터마크` / `숨긴 표시` 두 개. 화면을 옮기지 않고 저장 직전에 끌 수 있다.
상태 안내 문구가 같이 바뀌고, 세부 설정은 `자세히 설정 →` 로 워터마크 화면에 간다.

### ④ 글투 유형별 제목 추천 5가지 이상 — `feed.title_ideas()`
`news / magazine / brief / cards` 마다 결이 다른 후보를 **최소 5개** 준다.
재료는 전부 기사에서 뽑은 낱말이고 말투만 글투에 맞춘다.

**여기서 뿌리 문제를 두 개 찾아 고쳤다** — 원래 `summarizer` 가 만들던 것이라
**앞장 제목·후킹 문구 후보에도 똑같이 있던 문제**다.
- 🔴 **동사 토막이 제목 재료로 섞였다.** `topics()` 가 '확대하겠다고' 같은 것도 주는데
  틀에 그대로 끼워 `확대한다고까지`, `무산됐다, 투표율까지` 가 나왔다.
  → `summarizer.topic_words()` 신설(한 낱말 명사만) + `titles()`·`hooks()` 가 그걸 쓰게.
- 🔴 **조사가 받침을 안 맞췄다.** `정부 대학생가 움직인다`.
  → `summarizer.josa(word, pair)` 신설. 받침 여부로 이/가·은/는·을/를·과/와를 고른다.

### 아직 안 한 것
- `out\` 정리에 "휴지통으로 보내기" (지금은 영구 삭제만) — **손대지 말라는 지시**
- `_actor()` 가 '정부 대학생' 같은 두 낱말 묶음을 주체로 잡을 때가 있다.
  제목 후보에 `정부 대학생, 국토교통부 논의` 처럼 어색하게 남는다.

---

## 9. 2026-08-08 (3차) — 앱이 멎는 문제를 끝냈다

### 증상과 진짜 원인

바로가기로 띄우면 포트는 듣는데 응답이 없고 `netstat` 에 `CLOSE_WAIT` 가 쌓였다.

원인은 **콘솔 출력**이다. 윈도우 콘솔은 창 안이 클릭돼 글자가 선택되면 그 창으로
나가는 출력을 **멈춰 세운다**. 앱이 요청마다 그 창에 기록을 쓰고 있었으니, 요청을
처리하던 스레드가 전부 거기서 멈췄다. 응답을 못 하니 소켓이 `CLOSE_WAIT` 로 남는다.

### 🔴 어제 해 둔 조치는 **모자랐다**

`log_message` 만 끄고 끝낸 줄 알았는데, 시험해 보니 **똑같이 멎었다**(3/3 재현).
콘솔로 나가는 길이 하나가 아니었다.

| 나가는 길 | 언제 | 처리 |
|--|--|--|
| `log_message` | 요청마다 | 어제 껐음 (`--verbose` 로만) |
| `traceback.print_exc()` ×2 | 요청이 예외를 낼 때마다 | **이번에 `log_exc()` 로** |
| `socketserver.handle_error` | 핸들러 **밖으로** 샌 예외 | **이번에 `Server` 로 덮어씀** |

세 번째가 특히 안 보이는 길이었다. `do_GET`/`do_POST` 의 `try` 로는 못 잡는 경우
(응답을 쓰는 도중 연결이 끊기는 등)를 socketserver 가 제 손으로 stderr 에 찍는다.

### 고친 것 세 가지

1. **`disable_quickedit()`** — 시작할 때 콘솔의 빠른 편집을 꺼서 클릭해도 선택이
   안 되게 한다. 뿌리를 없애는 쪽.
2. **요청 경로에서 콘솔 출력 0** — 오류 자취는 `오류기록.txt` 로 (1MB 넘으면 새로).
3. **`Server.handle_error`** — 핸들러 밖으로 샌 예외까지 파일로.

### 어떻게 확인했나 — **클릭 없이 재현하는 시험을 만들었다**

사람이 창을 클릭해야 재현되는 문제는 반복 시험이 안 된다. 그래서 **아무도 읽지 않는
파이프**에 출력을 물렸다. 버퍼(4KB 남짓)가 차면 그 다음 출력은 막힌 콘솔과 똑같이
멈춘다. 원인이 같으니 증상도 같다.

두 가지로 봤다.

- **① 버티기** — 요청 300회(정상·예외·정상)를 던져 끝까지 응답하는가
- **② 0바이트** — 인사말 뒤로 **요청 때문에 새로 나온 콘솔 출력이 몇 바이트인가**

②가 핵심이다. ①은 4KB가 차야 막히는 약한 재현이지만, 진짜 콘솔은 **첫 글자에서**
막힌다. 그러니 "0바이트"여야 안전하다고 말할 수 있다. 한 글자도 안 쓰면 창이 막혀 있든
말든 상관이 없다.

| 시험 | 고치기 전 | 고친 뒤 |
|--|--|--|
| ① 요청 300회 | 예외 요청 **3번째에서 멎음** | 300회 전부 응답 (10/10회) |
| ② 요청 뒤 콘솔 출력 | **43,020바이트** (`--verbose` 대조군) | **0바이트** (10/10회) |
| 진짜 콘솔 창에서 띄우기 | — | 10회 × 60요청 전부 응답 |
| 실제 바로가기(.lnk) | 멎음 | 5회 × 60요청 전부 응답 |
| 배포본 ZIP 을 풀어 동봉 파이썬으로 | — | 5화면 × 40요청 = 200회 전부 응답 |

`--verbose` 를 대조군으로 남겨 둔 것이 값졌다. 고친 뒤에도 `--verbose` 는 여전히
멎는다 — 시험이 **아직 예민하다**는 증거다. 대조군이 통과해 버리면 시험이 아무것도
안 재는 것이다.

### 이번에 밟은 함정

- 🔴 **`p.stdout.read()` 로 출력을 세면 언제나 0이 나온다.** 프로세스가 끝날 때까지
  돌아오지 않기 때문이다. 도중에 세려면 `readline` 으로 조금씩 받아야 한다.
  하마터면 **고쳐지지 않았는데 0바이트라고 좋아할 뻔했다**(대조군이 0으로 나온 걸
  보고 알았다).
- **콘솔 창을 제목으로 찾으면 안 된다.** 실제 제목에는 `관리자:` 가 붙고 선택 중이면
  앞에 `선택` 까지 붙는다. 창 종류(`ConsoleWindowClass`) + PID 로 찾아야 한다.
- **BOM 없는 UTF-8 `.ps1` 은 PowerShell 5.1 이 한글을 깨뜨린다**(`Illegal characters
  in path`). 한글 경로를 다루는 스크립트는 파이썬으로 쓰는 편이 낫다.
- `ctypes` 는 **`restype`/`argtypes` 를 반드시 지정**한다. 안 하면 실패를 성공처럼
  읽는다.

### 못 한 것 — 정직하게

콘솔에 `표시(Mark)` 명령(`WM_SYSCOMMAND` 0xFFF1)을 보내 **선택 상태를 프로그램으로
만들어** 보려 했으나, 이 윈도우 11 빌드에서는 걸리지 않았다(제목에 `선택` 이 안 붙음).
그래서 **"클릭해서 멎는 상황" 자체를 그대로 재현한 증거는 없다.** 대신 그보다 넓은
것을 보였다 — 요청 경로에서 콘솔에 **한 바이트도 쓰지 않는다.** 쓰지 않으면 막힐
일도 없으므로 이 부류 전체가 사라진다.

### 확인된 것

- ZIP 63항목, 소스와 불일치 0. 갈아 끼운 항목은 `app.py` · `사용설명서.md` ·
  `★ 먼저 읽어주세요.txt` 셋뿐이고 나머지 60항목은 **건드리지 않았다**(§5-① 교훈대로).
- 시험 뒤 우리 서버의 `CLOSE_WAIT` 0개, 남은 `python.exe` 0개.


---

## 2026-08-09 (5차) — 브라우저를 안 띄우는 진짜 응용프로그램으로

### 왜 다시 손댔나

"완전한 응용프로그램인데 브라우저 기능을 전혀 안 썼으면 좋겠다"는 요구.
확인해 보니 **뉴보대는 exe 로 만든 적이 한 번도 없었다.** 바탕화면 바로가기는
처음부터 `카드뉴스 메이커 실행.bat` 을 가리켰고, 폴더 전체에 `.exe` · `.spec` ·
`앱\` · `build\` 가 하나도 없었으며 이 일지에도 pywebview 언급이 0줄이었다.
exe + WebView2 작업은 **경제 카드뉴스 메이커(2026-08-08)에만** 하고 뉴보대로는
옮기지 않았던 것이다. 되돌아간 게 아니라 애초에 안 넘어왔다.

### 먼저 잰 것 — 무엇을 갈아야 하는가

브라우저를 걷어내는 비용이 어디에 걸리는지부터 셌다.

| 무엇 | 어디 | 크기 |
|--|--|--|
| 앞장 카드 그리기 | `static/app.js` | canvas 호출 50 |
| 뒷장 5종 | `static/outro.js` | canvas 호출 94 |
| 워터마크 | `static/hiddenmark.js` | canvas 호출 17 |
| **파이썬 쪽 그리기 코드** | — | **0줄** |

그리는 일은 100% 브라우저 canvas 안에서 일어난다. 파이썬으로 옮기면 Pillow 가
필요해 "설치 0개"가 깨지고, 글자 폭을 재는 방식이 `measureText` →
`font.getlength` 로 바뀌어 카드 줄바꿈·자간이 전부 틀어진다. 뒷장 5종과
워터마크를 처음부터 다시 검증해야 한다.

→ **그리는 코드는 한 줄도 건드리지 않고 껍데기만 바꾸기로 했다.**

### 창을 세 단으로

`appwindow.py` 를 새로 두고 위에서부터 내려간다.

| 단 | 무엇 | 브라우저가 뜨나 | 언제 |
|--|--|--|--|
| 1 | pywebview + WebView2 | **안 뜬다** (제 프로세스가 제 창) | exe |
| 2 | 엣지·크롬 `--app=` | 뜬다 (주소창·탭은 없음) | 공유용 ZIP |
| 3 | 보통 브라우저 탭 | 뜬다 | 엔진 없는 PC |

중간에 2단만 만들어 놓고 시험해 봤는데, 창은 주소창 없이 잘 떴지만
**작업 표시줄에 브라우저가 하나 더 뜨는 것**은 그대로였다. 요구가
"브라우저 기능을 전혀"였으므로 1단까지 갔다.

### 🔴 부딪힌 것 넷

1. **WebView2 창은 주 스레드에서만 돈다.** `webview.start()` 는 창이 닫힐 때까지
   돌아오지 않는다. 그래서 `serve_forever()` 를 딴 스레드로 보내고 주 스레드를
   창에게 내줬다. 반대로 두면 창이 아예 안 뜬다.
2. **검은 창을 숨길지 말지는 창이 뜬 뒤에 물어볼 수 없다.** 1번 때문에 그때는
   이미 돌아오지 않는다. → `has_webview()` 로 **먼저** 묻고 숨긴 뒤 창을 띄운다.
   못 띄웠는데 숨기면 화면에 아무것도 없는 상태가 된다.
3. **PyInstaller 의 `--add-data` 도 `--specpath` 기준이다.** `--icon` `--splash` 만
   절대경로면 되는 줄 알았는데 `build\static` 을 찾다가 죽었다. 전부 절대경로로.
4. **`.NET ZipArchiveMode` 는 `Add-Type -AssemblyName System.IO.Compression.FileSystem`
   만으로는 안 잡힌다.** ZIP 갱신 9건이 **한 건도 안 쓰인 채** 성공 메시지만 찍혔다
   (`$zip` 이 null 인데 루프는 계속 돌아 "갈아끼움" 이 9줄 나왔다). 크기가 그대로인
   것으로 알아챘다. → 파이썬 `zipfile` 로 다시 했다. **성공 메시지를 믿지 말고
   결과 크기·해시를 볼 것.**

### 저장 자리 — exe 로 묶으면 갈라진다

`_MEIPASS`(exe 안) 와 결과물 자리를 갈랐다.

- `BUNDLE` = 화면·폰트·로고 (읽기만)
- `BASE` = `out\` · `오류기록.txt` · 인스타 그림 폴더 (쓴다)

🔴 `BASE` 를 exe 옆으로 두면 다시 구울 때마다 `앱\` 이 통째로 지워져 저장한 카드가
날아간다. 그래서 exe 자리에서 위로 올라가며 이미 쓰던 `newsfeed` 를 찾고, 없을
때(남에게 준 exe)만 exe 옆을 쓴다. 실측으로 `저장 폴더 = ...\뉴보대\newsfeed\out`
으로 잡혔고 `첫게시물`·`프로필_하이라이트` 도 그대로 찾았다.

(이 줄 자체가 함정 하나를 더 남겼다 — 파이썬 문자열 안에서 `\n` 은 경로가 아니라
줄바꿈이 된다. 한글 경로는 `\뉴` 처럼 escape 가 아니라 그냥 지나가서 눈치채기
어렵고, 하필 `newsfeed` 만 `n` 으로 시작해 잘렸다.)

### 확인한 것

| 시험 | 결과 |
|--|--|
| exe 실행 | 창 제목 `뉴보대 카드뉴스 메이커`, **창 주인이 exe 프로세스 자신** |
| 브라우저가 떴나 | `--app=` 크롬/엣지 프로세스 **0개** |
| 화면 6개 + 아이콘 + 글꼴 + 로고 | 전부 HTTP 200 |
| 저장 폴더 | exe 안이 아니라 `newsfeed\out` (다시 구워도 안 날아감) |
| 창 닫기 → 종료 | 0.5초 내 프로세스·포트 정리, 남은 WebView2 0 |
| 오류기록.txt | 안 생김 |
| 공유용 ZIP (pywebview 없음) | 2단으로 물러서 엣지 앱 창, 검은 창 숨김 확인 |
| ZIP 대조 | 소스 29개 일치·불일치 0, 배포 전용 4개 살아 있음 (67항목) |

### 남은 것

- exe 폴더 51.4MB · exe 6.45MB. 공유는 여전히 ZIP(17.5MB) 쪽이다.
- WebView2 첫 실행이 4~5초 걸려 시작 그림(`splash.png`)으로 덮었다.
  더 줄이지는 못했다.

---

## 2026-08-09 (6차) — 인스타 올리기 화면: 사진이 안 보이던 것 + 문구 고르는 단계

### 무엇이 문제였나

쓰다가 두 가지가 걸렸다.
1. 사진을 고르는데 **사진이 안 보인다** — 회색 네모에 파일 이름만 떴다.
2. 사진을 고른 뒤 **다음 단계가 없다.** 문구 칸이 빈 상자 하나뿐이라
   어디서 무엇을 가져와 채워야 하는지가 화면에 없었다.

### 왜 그랬나

①은 `insta.html` 이 `<img>` 를 만들면서 **`src` 를 아예 안 넣고 있었다.**
그 자리에 이런 주석이 있었다 — "미리보기는 기존 정적 경로로는 못 꺼내오니 파일명만
보여준다(가벼움 유지)". `/static/` 은 화면 파일 전용이라 `첫게시물\` 같은 바깥 폴더를
못 꺼내는 것은 맞다. **그러면 경로를 하나 여는 게 답인데 이름만 띄우고 넘어갔다.**
이름만으로는 `01_표지` 와 `02_시사` 를 못 가르니, 고르는 화면으로서 제 일을 못 한 셈이다.

②는 만드는 기능이 없어서가 아니라 **이어 붙이지 않아서**였다. 문구를 만드는
`feed.py` 는 이미 있었고 `피드 글` 화면도 있었다. 올리기 화면만 그걸 모르고 있었다.

### 고친 것

- `GET /api/insta-thumb?dir=&name=` — 그림을 실제로 내려 준다.
  꺼내 오는 폴더는 게시용 허용 목록(`_insta_roots`) 안으로 못 박았다
  (`C:\Windows`, `...\..` 둘 다 404 로 막히는 것을 시험에 걸어 뒀다).
- `POST /api/insta-captions` — **문구 후보 5개.** 앞장의 제목 추천과 같은 결이다.
  기사를 주면 글투 5가지(뉴스 전달형·짧은 브리핑·질문 던지기·매거진형·한 줄+태그),
  안 주면 **채울 자리를 대괄호로 남긴 틀 5개**(첫 게시물·오늘의 카드뉴스·질문·공지·한 줄).
  `feed.caption_ideas()` 로 넣었고, 없던 두 결(질문·한 줄)만 새로 썼다.
- `GET /api/insta-texts` — `피드 글` 화면에서 저장해 둔 `out\*.txt` 를 후보로 불러온다.
- 화면: 순서 배지·`←  →`·`×`, 이름으로 거르기, 새것 먼저/이름순, 비율 경고.

### 함정 — 고쳐 놓고 새로 만든 것

사진을 보이게 했더니 **화면이 멎었다.** 1080×1350 PNG 열다섯 장(20MB)을 그대로
`<img>` 에 걸었더니 그리는 쪽이 통째로 굳어, 스크린샷 요청이 30초 시간 초과로 죽었다.
(JS 왕복은 멀쩡했다 — 즉 **자바스크립트가 아니라 래스터라이저가 막힌 것**이다.
이걸 안 갈랐으면 "느리다"로 뭉뚱그렸을 것이다.)

줄여서 보내려면 파이썬 쪽에 Pillow 가 필요한데 이 도구는 **설치 0개**가 규칙이라
그 길은 막혀 있다. 그래서 브라우저 쪽에서 풀었다 —
`createImageBitmap(blob, {resizeWidth:220})` 은 **디코딩하면서** 줄여 주므로 큰 그림이
통째로 메모리에 올라오지 않는다. 여기에 ① 화면에 들어온 것만 ② 두 장씩만 ③ 줄인 것을
캐시. 15장 전부 그려도 화면이 안 굳는다.

비율 경고는 원래 그림이 다 뜬 뒤에야 잴 수 있었는데, **파일 머리표만 읽어** 가로·세로를
서버가 재도록 했다(`_img_size`, PNG IHDR / JPEG SOFn / WEBP VP8X, 전부 표준 라이브러리).
덕분에 **고르는 즉시** "비율이 다른 그림이 n장 섞였습니다" 가 뜬다. 모르는 형식은
0 으로 두고 넘어간다(터지지 않는다).

- JPEG 파서에서 `0xC4`(DHT)·`0xC8`·`0xCC` 는 SOF 가 아니다. 안 걸러내면 엉뚱한 값을
  크기라고 읽는다. 우리 폴더엔 JPEG 이 없어 **머리표를 손으로 만들어** 시험에 넣었다.

### 확인한 것

`시험_인스타화면.py` 신설 — 크롬·인스타 없이 서버만 띄워서 돈다. **27개 전부 통과.**
그림 바이트가 진짜 오는가 / 허용 폴더 밖은 404 인가 / 후보가 5개이고 서로 다른가 /
기사 사실이 실제로 들어가는가 / 짧은 후보가 하나는 있는가(전부 긴 초안이면 안 된다) /
저장해 둔 글이 내용까지 오는가 / 머리표 크기(PNG·JPEG·WEBP·이상한 파일).

실제 화면에서도 확인했다 — 썸네일 15장이 다 뜨고, 크기(1080×1350 / 1080×1920)가
불러오기 전에 뜨고, 4:5 에 9:16 을 섞으면 어느 파일이 다른지 이름으로 짚어 준다.
기사 본문을 넣으면 후보 5개(363·250·312·337·191자)가 나오고 누르면 아래 칸에 들어간다.

`피드 글` 화면 회귀도 봤다(4가지 글투 전부 ok, `MINE` 을 모듈 상수로 올린 뒤에도
`brief` 만 그 줄이 없는 것은 원래 그랬다).

### 산출물

exe 재빌드 + 공유 ZIP 갱신(68항목, 17.6MB — `app.py`·`feed.py`·`static/insta.html`·
`사용설명서.md` 갈아끼우고 `시험_인스타화면.py` 추가, 바이트 대조 통과).
빌드 때 실행 중이던 앱을 닫아야 했다(`_내부\clr_loader\...ClrLoader.dll` 잠김) —
닫고 굽고 다시 띄웠다.

---

## 2026-08-09 (7차) — 링크 이어가기 · 저장 진행바 · 저장 없이 올리기 · 머리글 통일

지시 네 가지. 서로 떨어져 보이지만 **"화면을 넘을 때마다 손으로 다시 채우는 일"** 이라는
한 뿌리였다 — 주소를 세 번 붙여 넣고, 문구를 복사해 옮기고, 카드를 저장했다가 다시 고른다.

### ① 기사 링크가 따라간다

한 화면에 넣으면 `nb_url` 에 남고, `피드 글`·`인스타 올리기` 링크의 `href` 에 `?url=` 로
미리 박아 둔다. **클릭할 때 고치지 않았다** — 가운데 클릭·키보드로 열면 안 먹기 때문.
받는 쪽 규칙: **`?url=` 로 들어온 경우에만 기사를 가져오고**, 그냥 이어받은 것이면 칸만
채운다(화면 열 때마다 언론사에 요청을 보내면 안 된다).

### ② 저장 진행바 — 단추 바로 아래

`static/prog.js`. `PROG.start(단추)` 가 `.prog-wrap` 으로 감싸고 막대를 **절대좌표**로
붙인다(막대가 뜰 때 머리글이 밀리면 저장 도중 단추가 움직인다).

🔴 **% 를 지어내지 않는다.** 실제로 끝난 단계에서만 올라가고, 보내는 구간만 바이트로
잰다(`XMLHttpRequest.upload.onprogress` — `fetch` 로는 못 잰다). 저 혼자 흐르는 막대는
"도는 중"과 "멈춤"을 구별하지 못하게 만들어 없느니만 못하다.
`5종 모두 저장` 은 진짜 몇 장 중 몇 장이 있으므로 장 단위로 구간을 나눠 준다.

🔑 **함정: 캔버스 일은 주 스레드를 잡는다.** 워터마크 심기·`toDataURL` 사이에 막대를
갱신해 봐야 화면이 안 그려져 0% 에 있다가 100% 로 튄다 → 단계 사이에 한 프레임 쉰다
(`await breathe()` = rAF + setTimeout 0).

### ③ 저장하지 않고 올리기

앞장·뒷장 머리글의 인스타 단추가 **지금 캔버스**를 그대로 가져간다.
인스타에 넣는 마지막 손잡이가 `DOM.setFileInputFiles` 라 **진짜 파일 경로가 있어야
하므로** 파일 자체는 반드시 생긴다 — 그걸 `out\` 에 두면 저장한 카드와 섞이니
`out\_임시_인스타\` 에 두고 목록에 `지금 화면(임시)` 로 띄운다. 넘어가자마자 **저절로
골라져 있고** 문구 칸에는 `피드 글` 화면의 글이 들어와 있다.

🔴 다시 담으면 **갈아 끼운다.** 안 지우면 눌렀던 만큼 쌓여 **지난 카드가 같이 올라간다** —
조용히 틀리는 쪽이라 더 나쁘다. 워터마크는 저장 때와 같은 `outCanvas()` 를 거치므로
결과물이 갈리지 않는다.

### ④ 머리글 통일

`피드 글` · `뒷장 만들기` **오른쪽**, `PNG 내려받기` **왼쪽**. 자리는 각 화면이
`<span data-insta-slot>` 으로 표시하고 **단추 자체는 `static/nav.js` 가 한 번만** 만든다
(로고는 인라인 SVG + 인스타 그러데이션). 화면마다 markup 을 복사하면 반드시 갈라진다 —
실제로 고치기 전 다섯 화면의 머리글 순서가 전부 달랐다.

### 확인한 것

- `시험_인스타화면.py` **62항목**(27 → 62). 임시 자리(담기·갈아끼우기·지우기·거절) +
  **머리글 자리를 파일에서 직접 대조**(슬롯이 PNG 왼쪽인가, 피드 글 오른쪽인가).
- 헤드리스 크롬으로 실제 화면 **34항목** — 브라우저 확장이 안 붙어 `insta.py` 의 CDP 길을
  그대로 썼다(전용 포트·프로필이라 인스타 로그인 창은 안 건드림).
- 🔴 **시험이 두 번 거짓으로 실패했고 둘 다 재는 쪽 문제였다.**
  ① `setInterval` 로 막대를 훔쳐보면 **캔버스가 주 스레드를 잡는 동안 타이머가 아예 안
  돈다** — 막대는 그려져 있는데 표본만 빈다. → 막대에 **무엇을 시켰는지**를 근원에서
  받아 적고, 화면이 실제로 그려졌는지는 단계 사이에 rAF 프레임이 지나갔는지로 따로 봤다.
  ② `let picks` 는 `window` 에 안 붙는다(스크립트 렉시컬 스코프 — 5차의 `const HIDDEN`
  과 같은 함정). → 화면에 골라진 표시(`.th.on`·`.pk`)를 세는 쪽으로 바꿨다.
- 눈으로도 확인: 머리글에 인스타 로고 + `보내는 중 81%` 막대가 `out 폴더에 저장` 아래.

산출물: exe 재빌드 + ZIP **70항목**(바꾼 것 12 · 새로 넣은 것 `nav.js`·`prog.js`).

---

## 2026-08-09 (8차) — "영 아니네" — 흐름을 사용자처럼 걸어 보고 두 군데를 고침

사용자 지적: **"사진 고른 뒤에 피드글 확인·수정하고 실제 업로드까지 되는 프로세스를
만들었다고 했는데 지금 보니 영 아니네."** 맞는 지적이었다.

### 무엇을 잘못했나

7차에서 조각은 다 시험했다 — 임시 자리에 담기, 캡션 후보 API, 올리기 배관.
**전체를 사용자처럼 한 번도 안 걸어 봤다.** 조각이 다 통과하면 길도 통한다고 여겼다.

걸어 보니(`scratchpad/walk.py`) 이렇게 나왔다.

| 걸어 본 것 | 실제 |
|--|--|
| 사진 담고 → 피드글 확인·수정 | 문구 칸 **0자**, 후보 **0개**, 기사 주소도 안 넘어옴 |
| 여러 장(캐러셀) | 앞장 담고 뒷장 담으면 **앞장이 사라짐** |

### 왜 그랬나

1. **문구가 빈손이었다.** `nb_feed_text` 는 `피드 글` 화면을 거쳐야만 생긴다. 앞장에서
   카드 만들고 바로 올리기로 가는 흔한 길에서는 늘 비어 있었다. 후보도 사람이
   `문구 5개 추천` 을 눌러야만 나왔다. 결국 '문구 고르기' 단계가 **빈 상자**였다.
2. **캐러셀을 못 만들었다.** 담기를 `replace` 로 만들었다. "지금 화면" 이라는 말에
   충실했지만, 인스타 게시물은 보통 여러 장이라 **길 자체가 막혔다.**

### 고친 것

- 담기를 **쌓기(append)** 로. 만들 때마다 누르면 그게 캐러셀 순서(이름 앞 번호).
  10장을 넘으면 막고, 비우는 것은 `임시 그림 지우기` 가 맡는다.
- 도착하면 **담긴 것 전부**를 번호 순서로 골라 둔다(이번에 담은 것만이 아니라).
  임시 그룹은 정렬 설정과 무관하게 번호 순으로 보여 준다 — 캐러셀 순서니까.
- 문구를 빈손으로 안 보낸다: ① 피드 글 ② 없으면 **카드에 적은 글**(후킹·제목·요약문)로
  만든 초안. 그리고 **들어가자마자 후보 5개를 저절로 깐다**(기사 주소가 있으면 그 기사로,
  없으면 빈 틀). 초안은 카드에 이미 있는 말이므로 "그대로 올리지 말라"고 화면이 말한다.
- 사진 안내와 문구 안내를 **다른 자리**로 갈랐다(같은 자리에 쓰니 후보 뽑기가 덮었다).

### 마지막 구간 — 진짜 인스타 화면에서 확인 (사용자 승인)

여태 올리기는 **가짜 시험 페이지로만** 확인했고 진짜 화면에서는 로그인 상태만 봤다.
승인을 받아 `dry_run` 을 돌렸다: 만들기 → 사진 2장 → 다음 → 문구 109자(칸에서 109자
확인) → **`공유하기` 앞에서 멈춤.** 통과했다.

🔴 **여기서 내가 한 번 틀렸다.** 공유 화면 미리보기를 보고 "사진이 잘려 들어간다"고
판단해 자르기를 자동으로 고르는 코드까지 넣었다. 그런데 **숫자로 재 보니 틀 비율이
0.800(=4:5, 원본 그대로)** 이었다 — 고르기 전에도. 좁은 미리보기 칸이 그림을 잘라
보여 준 것을 잘린 결과로 오독했다. 눈으로 본 것을 재 보지 않고 고친 것이 잘못이다.

남긴 것은 **재는 쪽**이다(고치는 쪽이 아니라):
- `crop_ratio()` 로 자르기 틀을 재고 원본 비율(첫 장, `_img_size` 로 잼)과 대조한다.
- 같으면 **손대지 않는다.** 맞는데도 목록을 여닫으면 `다음` 을 한 번 더 쓰게 된다(실측 2→3).
- 다를 때만 `원본` 을 고르고 다시 잰다. 그래도 다르면 🔴 로 크게 알리고 결과에 표시해
  화면이 경고한다(조용히 잘리는 것이 가장 나쁘다).
- 실측: 4:5 카드 0.800 / 9:16 커버 0.561 — 둘 다 원본과 같아 손 안 댐, `다음` 2번.

### 확인한 것

`시험_인스타화면.py` 62항목(쌓기·상한 10장·순서 유지로 갱신) 전부 통과.
실제 흐름 재주행: 담아 온 1장 자동 선택 · 문구 칸 48자(카드 글 초안) · 후보 5개 ·
앞장+뒷장 2장이 `01 → 02` 순서로 쌓임. 진짜 인스타에서 시험 실행 2회 통과.

산출물: exe 재빌드 + ZIP 70항목(`insta.py` 포함 15개 갱신).

## 2026-08-09 (9차) — "한 번 열면 기사 하나" — 지난 기사가 조용히 남아 있었다

사용자 지적: **"실행 한 번 하면 링크 무한정 적용 가능하게 해봐. 지금은 한 번 열면
하나만 적용되는 거 같은데?"** 맞았다. 재 봤더니 정확히 그랬다.

### 무엇이 문제였나 (숫자로 잼)

두 번째 링크를 넣으면 이렇게 나왔다.

| 무엇 | 두 번째 링크 뒤 |
|--|--|
| 사진 | 새 기사 것 18장 ✅ |
| 출처·날짜 | 새 기사 것 ✅ |
| **본문** | 🔴 첫 기사 그대로 |
| **원문 제목** | 🔴 첫 기사 그대로 |
| **문구 후보(후킹·제목·요약)** | 🔴 첫 기사 그대로 |

즉 카드에 **첫 기사 글 위에 두 번째 기사 사진**이 얹힌다. 빈 화면이 나오는 것보다
나쁘다 — 그럴듯해 보이므로 그대로 올라간다.

### 왜 그랬나

`/api/extract` 는 본문을 같이 주면 **그쪽을 우선**한다(`app.py:294`). URL 을 막는
언론사를 위해 일부러 그렇게 만든 길이다. 그런데 화면이 `가져오기` 를 누를 때마다

    api('/api/extract', { url, text: inBody.value, title: inTitle.value })

처럼 **칸에 남아 있는 지난 기사 본문을 같이 보내고 있었다.** 서버는 시킨 대로
그 본문을 썼다. 잘못은 서버가 아니라 **화면이 지난 기사를 못 놓은 것**이다.

🔑 서버만 두들기는 시험으로는 절대 안 잡힌다. `/api/extract` 는 링크를 몇 개를 주든
멀쩡히 답했다(실제로 그것부터 확인했다 — 두 링크 다 통과). **화면을 걸어야 보였다.**

### 고친 것

**① 우리가 채운 글은 주소가 바뀌면 스스로 물러난다** (`AUTO`)

칸에 있는 글이 *우리가 넣은 것*인지 *사람이 손으로 붙여 넣은 것*인지를 기억한다.
주소가 바뀌었고 우리가 넣은 것이면 비우고, **사람이 쓴 것은 절대 안 지운다** —
URL 막힌 언론사에 본문을 붙여 넣는 것은 정상 사용법이라 지우면 기능 하나가 죽는다.
대신 그 경우에는 화면이 말한다: "🔴 본문 칸에 직접 넣은 글을 그대로 썼습니다".
같이 물러나는 것: 고른 사진(`mixed`) · 저장 이름(사람이 적은 이름은 유지) ·
🔴 **피드 글**(안 지우면 지난 기사 캡션이 다음 게시물에 딸려 올라간다).

**② `새 기사` 단추 — 글·사진은 비우고 디자인은 남긴다**

`초기화` 는 디자인까지 되돌린다. 기사마다 테마를 다시 잡아야 하면 결국 앱을 다시
띄우게 되므로, 기사에 딸린 것만 터는 단추를 따로 뒀다(`newArticle()`).

**③ `피드 글` 화면도 같은 함정이었다** (`feed.html:178`) — 같은 규칙을 넣고,
만들어 둔 글까지 비운다.

### 확인한 것

`시험_링크이어넣기.py` 신설 — **헤드리스 크롬으로 화면을 직접 걷는다** (전용 포트·
전용 프로필이라 평소 크롬·인스타 로그인을 안 건드린다). **37항목 전부 통과.**

링크 A→B→C→A 로 이어 넣으며 본문 681자→4,973자→312자→681자로 실제로 갈리는지,
문구 후보·카드 제목·저장 이름이 매번 새로 나오는지 / 같은 링크를 두 번 눌러도
탈나지 않는지 / 손으로 넣은 본문은 살아남고 경고가 뜨는지 / 지난 피드 글이 비는지 /
`새 기사` 뒤 **테마 `#061634`·글자 크기 120 이 그대로**인지(디자인 보존) /
비운 뒤에도 링크가 또 들어가는지 / `초기화` 는 여전히 디자인까지 되돌리는지 /
피드 글 화면도 링크를 이어 받는지.

회귀: `시험_인스타화면.py` 62항목 · `시험_인스타.py` 전부 통과.
여섯 화면 연기 시험(콘솔 오류 0) — 끊긴 세션이 남긴 `outrostate.js`(뒷장 설정을
`뒷장 만들기`·`인스타 올리기` 가 같이 쓰게 떼어낸 것)도 두 화면에서 살아 있음을 확인.

### 함정 — 내가 만든 것

🔴 **ZIP 갱신 스크립트가 최상위 폴더 이름을 빼먹었다.** ZIP 안 경로는
`뉴보대 카드뉴스 메이커/static/app.js` 인데 `static/app.js` 로 찾으니 **70항목 전부가
'짝 없는 배포 전용'** 으로 보였고, 뿌리에 사본 34개가 더 붙어 70→104항목·18.4→25.8MB
가 됐다. 항목 수와 크기를 찍어 두지 않았으면 "성공"으로 넘어갔을 일이다
(5차 교훈이 그대로 반복됐다 — **성공 메시지 말고 크기·해시를 본다**).
백업(`.bak2`)에서 되돌리고 접두사를 붙여 다시 했다.

연기 시험도 한 번 거짓으로 실패했는데 재는 쪽이 틀렸다 — 전역 이름이
`OUTROSTATE` 인 줄 알았으나 실제로는 `OUTROST` 였다. 화면은 멀쩡했다.

### 산출물

exe 재빌드(`앱\`, 번들 안 `static` 이 소스와 해시 일치 확인) + 공유 ZIP **72항목**
(`static/app.js`·`index.html`·`feed.html`·`insta.html`·`outro.html`·`app.py`·
`사용설명서.md` 갈아끼우고 `시험_링크이어넣기.py`·`static/outrostate.js` 추가,
소스 34개 바이트 대조 불일치 0). exe 를 `--no-browser` 로 띄워 **번들이 실제로
새 화면을 내주는지**까지 확인했다(`id="btnNew"` · `newArticle` · `AUTO`).

### 함정 하나 더 — 시험이 남의 크롬에 붙었다

새로 만든 시험을 **CDP 포트 9333** 으로 잡았는데, 그건 `insta.py` 가 쓰는 번호다.
그 크롬(인스타 로그인용 전용 프로필)이 마침 떠 있어서 시험이 **제 크롬을 안 띄우고
남의 창에 붙었고**, 탭을 앱 주소로 보낸 뒤 끝에 닫아 버렸다. 로그인은 프로필 폴더에
남으니 다시 띄우면 그대로지만, 남의 창을 건드린 것은 명백한 잘못이다.

→ 시험 포트를 **9345** 로 옮기고, 그 번호를 이미 누가 쓰고 있으면 **붙지 않고
그만둔다**(`sys.exit(2)`). "떠 있으면 재활용" 은 인스타 올리기에서는 맞는 규칙이지만
(사람이 로그인해 둔 창을 써야 하니까) **시험에서는 정반대로 위험하다.**


## 2026-08-11 (10차) — 카톡 공유용 exe ZIP 「뉴보대 카드뉴스 메이커 (앱).zip」 신설

바탕화면 바로가기가 가리키는 exe판(앱\뉴보대 카드뉴스 메이커\, 51.4MB)을 다른 컴퓨터에서
압축만 풀면 바로 실행되는 공유 ZIP으로 묶었다. 기존 「뉴보대 카드뉴스 메이커.zip」(18.4MB)은
내장 파이썬+소스판이고, 이번 것은 exe판 — **공유 ZIP이 두 개가 됐다. 소스를 고치면 이제
exe 재빌드 + 소스 ZIP + 앱 ZIP 세 개를 다 갱신해야 한다.**

### 만들기 전에 확인한 것
- 소스(newsfeed\) ↔ 기존 ZIP: 최신 7파일(시험_링크이어넣기.py·outrostate.js 포함) 해시 전부 일치.
- 소스 ↔ exe 번들(_내부\static): 13파일 크기 전부 일치. 9차 빌드가 최신 그대로라 재빌드 없이 묶음.

### 내용물
앱 폴더 원본은 안 건드리고(재빌드하면 날아가는 위치) ZIP 안에만 2개 추가:
「★ 먼저 읽어주세요.txt」(압축 풀기→exe 실행, SmartScreen 추가정보→실행, _내부 지우지 말 것)
+ 「사용설명서.md」(newsfeed 것 복사). 총 333항목(원본 331+2), 27.2MB — 카톡 한도 안.

### 함정 2개
1. **.NET의 ZipFile::CreateFromDirectory 는 항목 이름을 역슬래시(\)로 쓴다.** 뒤에 내가
   슬래시(/)로 2개를 추가하니 구분자가 섞였다 — 압축 프로그램에 따라 폴더가 둘로 갈라져
   보일 수 있다. 파일별 CreateEntryFromFile 로 전부 / 통일해 다시 만들었다(역슬래시 0 확인).
2. CLI 권한 검사기가 명령 문자열 속 역슬래시 치환 인자와 일지 본문의 삭제 명령어 단어를
   실제 삭제 경로로 오인해 두 번 차단했다. 삭제는 별도 호출로 빼고, 긴 한글 본문은
   파일로 먼저 쓴 뒤 이어붙이는 방식으로 통과.

### 산출물
「뉴보대 카드뉴스 메이커 (앱).zip」 27.2MB — 항목 수 333 대조·exe SHA256 원본 일치 확인.


### 10차 덧붙임 (같은 날) — 구 공유 ZIP 폐기, 공유 산출물은 「(앱).zip」 하나로

사용자 지시로 소스판 「뉴보대 카드뉴스 메이커.zip」(17.6MB)과 「.zip.bak」(17.5MB)을
삭제했다. **이제 공유 ZIP은 「뉴보대 카드뉴스 메이커 (앱).zip」 하나뿐이다** — 위에서
"셋 다 갱신"이라 적었지만 몇 시간 만에 둘로 줄었다(exe 재빌드 → 앱 ZIP 갱신).
소스판이 다시 필요하면 newsfeed\ 에서 그때 새로 굽는다(내장 파이썬 동봉 방식은 작업일지 앞부분 참고).

바탕화면 바로가기는 앱\ 폴더의 exe 를 가리키고 exe 는 9차 빌드 그대로 최신이라
**갱신할 것이 없음을 확인**만 했다(대상 존재·수정시각 08-09 15:13 일치).


## 11차 — 2026-08-12: 스타일 모드 6종 + 제목 폰트 세대 교체

사용자 지적 두 가지: "폰트가 진짜 못생겼다"(전 층이 Pretendard 하나 — 제목까지
본문 글꼴이라 밋밋), "각 회사별 특징이 제대로 들어가있지 않아"(색 테마 5종은
색만 갈아끼움 — 잘나가는 계정들의 결(타이포·레이아웃·장식)이 없음).

### 만든 것
- **폰트 4종 동봉**(전부 상업용 무료, 설치 0개 유지): 지마켓 산스(Medium/Bold woff),
  에스코어 드림(5/7/9 woff), 검은고딕(ttf), 페이퍼로지(5Medium/8ExtraBold woff2).
  눈누 jsdelivr CDN + 구글폰트 GitHub에서 수급, 매직바이트 검증.
  style.css @font-face는 굵기를 **구간**(100 599 / 600 900)으로 걸어 가짜 볼드 방지.
- **기본 제목 글꼴 교체**: Pretendard Black 900/-32 → **지마켓 산스 700/-16**
  (자간 -32는 Pretendard 전용 튜닝이었다. 지마켓 산스는 폭이 넓어 -16이 적정).
- **스타일 모드 6종**(app.js STYLES + index.html #styles): 뉴닉풍(흰 종이+오렌지 태그바+
  지마켓 산스) / 스브스뉴스풍(사진 풀블리드+파랑 배지+검은고딕 96px) / 어피티풍
  (레터헤드 레드 띠+에스코어 드림) / 크랩풍(먹빛+마젠타 알약+시안 강조) / 캐릿풍
  (앰버 태그+페이퍼로지) / 토스풍(여백+블루 한 점+Pretendard Black).
  색 테마와 달리 **글꼴·자간·박스·그림자·바탕·장식·화살표 색을 통째로** 정한다.
- 상태에 `paper`(사진 없을 때 바탕색)·`deco`(tag|band)·`decoColor` 추가.
  render()가 paper로 깔고 drawDeco()를 오버레이 다음·글자 앞에 그린다.
  색 테마 클릭 시 paper 동기화 + deco 끔.

### 🔑 브랜드 색은 로고에서 실측했다 — 내 기억이 두 번 틀렸다
favicon·로고 PNG·유튜브 아바타를 받아 PIL로 지배색을 쟀다:
뉴닉 **#ff6b00**(보라라고 기억했었다 — 오렌지가 맞음) / 어피티 **#ff441f**(노랑인 줄
알았다 — 레드가 맞음, 노랑은 머니레터 서브) / 스브스뉴스 **#0050f8**(핑크 아님) /
크랩 **#f80090+#40d0f8**(네온그린 아님) / 캐릿 **#f8a800** / 토스 #3182f6(경제 메이커
themes.py 실측치 재사용). **기억으로 팔레트를 짰으면 "특징이 안 들어갔다"는 지적을
그대로 반복할 뻔했다.**

### 시험
- `시험_스타일모드.py` 신설 **32항목 전부 통과**(CDP 포트 9346 — 9333 인스타·9345
  링크시험과 분리). 폰트 6파일 매직바이트 → 기본값 지마켓 산스 로드 →
  모드 6개 클릭마다 상태+**캔버스 픽셀**(밝기·태그바 색) → 색 테마 초기화 →
  새로고침 복원까지. 모드별 미리보기 PNG는 `out\_스타일미리보기\`.
- 회귀: `시험_인스타화면.py` 62항목 전부 통과.
- 함정 재발: 시험 스크립트도 파이프에 물리면 cp949 로 죽는다(8차와 동일) —
  스크립트 첫머리 `reconfigure(errors="replace")`가 이제 상비약.
- 헛짚음 하나: 스크린샷 축소본에서 숨은 워터마크 무늬가 패널까지 번져 보여
  코드 결함을 의심했으나, 원본 crop 확대로 **캔버스 안에만 있음**을 확인(눈으로 본
  것을 재보기 전에 고치지 말 것 — 9차 교훈이 이번엔 지켜짐).

### 산출물 (셋 다 확인)
- exe 재빌드(00:40) — 번들 fonts 14파일·새 static 서빙 실측(--no-browser로 띄워 확인).
- 「(앱).zip」 **341항목·31.3MB**(+8 = 새 폰트, 역슬래시 0, app.js 소스=번들 해시 일치).
  ★txt는 구 ZIP에서 승계, 사용설명서는 스타일 모드 단락 추가판.
- 바탕화면 .lnk → 새 exe 대상 존재 확인.


## 12차 - 2026-08-13: 주제 찾기·참고 사이트·pure 판 + 산출물 세대 갱신

(오전에 다른 세션이 hub.py·topics.html·시험_주제찾기.py 까지 만들고 네트워크가
끊겨 멈췄던 작업을 이어받아 끝냈다. 그 세션 몫의 기록도 여기 함께 남긴다.)

### 만든 것
- **주제 허브(topics.html + hub.py)** [오전 세션]: 참고 계정·매체 25곳(유튜브 채널
  RSS·사이트 RSS, 전부 키 불필요)의 최신 글/영상 제목을 모아 후보로 내놓고,
  고른 주제로 구글·빙 뉴스에서 국내 기사를 찾아 앞장으로 넘긴다.
  유튜브 연속 요청 500 대비 세마포어 3 + 10분 캐시.
- **오늘의 주제 후보(/api/topic-ideas)**: 구글 뉴스 갈래(섹션) RSS 6종
  (종합·경제·정치·사회·세계·IT·과학)을 related.topic_ideas() 로 받아 topics.html
  맨 위에 칩으로 얹었다. 실측: 여섯 갈래 전부 30~70건, 한국판에 POLITICS 섹션이
  실제로 있다. 서버 10분 캐시. 링크는 구글 중계 주소라 **제목·언론사·시각까지만
  보장** - 화면 문구에도 그렇게 적었다.
- **참고 사이트(refs.html + refs.js)**: 「뉴보대 및 경제 참고 웹사이트.txt」에서
  **(뉴보대) 태그 항목만** 구조화해 17브랜드 61링크. 용도(주제 찾기/카드제작/
  글 작성/리서치·인사이트)·플랫폼·브랜드 필터, 전부 새 탭. (경제)만 붙은 항목
  (corp.tossinvest, 미래에셋 리서치, cnbcselect, wsjopinion 등)은 넣지 않았고
  시험이 이 제외까지 확인한다. 왼쪽에 카드 제작 팁 4가지 상자.
- **머리글 중앙화(nav.js)**: 오전 세션이 '주제 찾기' 링크를 여섯 화면에 복사해
  뒀던 것을 회수 - 화면 링크(주제 찾기·참고 사이트)는 인스타 단추처럼 nav.js 가
  슬롯에서 한 번만 만든다. 지금 화면으로 가는 링크는 안 만들고, 인스타 화면처럼
  단추가 필요 없는 곳은 `data-nav-slot`(링크만).
- **제목 이어가기(?title=)**: 주제 후보/기사 찾기에서 앞장으로 갈 때 주소와 함께
  제목을 싣는다. 구글 중계 주소는 본문 추출이 실패하는데, 그때도 정확한 제목이
  원문 제목 칸에 남아 제목 후보·본문 분석으로 이어진다. /api/extract 는 원래
  넘겨준 제목을 우선하므로 서버 수정 없이 화면 배선만으로 됐다.
- **pure 판**(사용자 추가 요청 "모드 없는 예전 버전"): 스타일 모드 6종 없는 기본
  디자인 판. **소스는 안 가르고 스위치 하나** - `--pure`/`NB_PURE=1`/exe 이름에
  pure. 화면은 `/api/config.js`(동기 <script>, fetch 로는 단추 만들기 전에 못
  받는다)로 스위치를 받아 스타일 모드 단추를 아예 안 만든다. 상태 열쇠도
  `nb_state_pure` 로 갈라 본판 모드가 pure 로 안 스며든다(시험으로 확인).
  pure exe 는 따로 굽지 않고 **본판 앱 폴더 복사 + exe 개명**(내용이 같으니).

### 시험 (전부 통과)
- 시험_주제찾기.py 53개(허브+refs+topic-ideas+pure 스위치+회귀)
- 시험_주제흐름.py 22개 신설 - refs 그리기→주제 후보 칩→고르기→앞장 도착까지
  **화면으로 끝에서 끝까지** + pure 판 단추 0개·상태 격리. CDP 9345 헤드리스.
- 회귀: 시험_링크이어넣기 37 / 시험_스타일모드 32 / 시험_인스타화면 62 / 시험_인스타

### 오판 2 + 낡은 시험 1
1. **백그라운드로 회귀시험을 돌려 둔 채 app.js 를 고쳤다** - 서버가 고치던 파일을
   그대로 내보내 가짜 실패 8건. 파일을 만지는 동안엔 그 파일을 서빙하는 시험을
   같이 돌리지 말 것. 재실행으로 전부 해소.
2. 시험 둘을 동시에 돌리면 헤드리스 크롬 백그라운드 탭의 타이머가 늦어져
   저장 디바운스(400ms)가 새로고침에 밀리는 플레이크가 난다(스타일모드 [5]).
   단독 재실행은 통과. CDP 포트만 갈라서는 부족하고 **직렬로 돌리는 게 맞다**.
3. 시험_링크이어넣기의 기본 제목 크기 기대값이 86으로 낡아 있었다(11차에서
   지마켓 산스로 바꾸며 84가 됐는데 시험만 안 고침). 시험 쪽을 84로 수정.

### 산출물 (둘 다 셋 확인)
- 본판: exe 재빌드(smoke: config pure=false·refs·topics·hub-sources 실측) +
  「(앱).zip」 **345항목·31.3MB**(이전 342·31.3MB, 그대로 335 / 바뀜 7 = exe·
  base_library·app.js·index·insta·nav.js·사용설명서 / 새로 3 = refs.html·refs.js·
  topics.html, 역슬래시 0, ★txt 는 구 ZIP 승계) + 바탕화면 .lnk 대상 확인.
- pure 판: 앱\뉴보대 카드뉴스 메이커 (pure)\(56MB, smoke: config pure=true) +
  「뉴보대 카드뉴스 메이커 (pure)(앱).zip」 **345항목·31.3MB**(★txt 는 pure 문구로
  바꿔 담음) + 「뉴보대 카드뉴스 메이커 (pure).lnk」 신설.
- 소스 static 16파일 = 본판 번들 = pure 번들 해시 전부 일치.


## 13차 - 2026-08-14: 포트 겹침 버그(본판·pure 동시 실행) + 릴스 만들기

### 버그 - "pure 를 켰는데 본판 화면이 나온다"
사용자가 "구현이 제대로 되지 않는 것 같다"고 했다. 서버 API·시험·해시 대조까지
전부 멀쩡해서 한참 헤맸는데, 진짜 병은 **본판이 떠 있는 채 pure 를 켜는 순간**에만
나타났다: HTTPServer 기본값(SO_REUSEADDR)은 윈도우에서 **이미 쓰는 포트에도
그냥 붙는다.** OSError 가 안 나니 "다음 포트로 비켜 가기" 코드가 영영 안 탔고,
두 번째 창은 첫 번째 프로세스의 화면을 그대로 보여 줬다(pure 를 켰는데 스타일
모드가 보이는 식). 실측으로 재현 후 `Server.allow_reuse_address = False` 로 수정 -
이제 둘째 인스턴스가 7871 로 비켜 가 제 화면을 낸다(exe 로 끝단 확인).
경제 카드뉴스 메이커도 같은 병이라 같이 고침. 시험_릴스.py·시험_차트후보.py 에
재발 감시 추가.

🔑 교훈: 시험이 전부 통과해도 **"동시에 두 개"는 아무도 안 재고 있었다.**
사용자의 재현 조건(어제 쓰던 창을 안 닫고 새 걸 켬)을 그대로 흉내내야 보였다.

### 릴스 만들기 (사용자 요청: 언론사 공식 릴스 참고)
- reel.html 신설 + nav.js 링크. 연합뉴스·YTN 릴스 관행: 카드 가운데 + 카드를
  흐려 채운 바탕 + 켄 번즈 확대 + 0.35초 크로스페이드 + 아래 진행줄, 1080×1920
  30fps. 소리는 안 넣는다(업로드하며 인스타 앱에서 유행 음악 - 그쪽이 관행).
- 만드는 쪽은 화면(canvas + MediaRecorder). ffmpeg 동봉 없음(설치 0개 유지).
  서버는 /api/reel-save 로 바이트만 받아 out\릴스_<시각>.mp4 에 놓는다
  (JSON base64 아님 - 30MB 영상이 40MB 글자가 되는 것을 피함).
- 흐린 바탕은 장마다 **미리** 구워 둔다 - 매 프레임 blur 는 GPU 없는 노트북에서
  프레임이 끊긴다.

### 🔴 isTypeSupported 를 믿으면 안 된다
헤드리스 크롬에서 mp4 가 "지원한다"고 답하고 **0바이트**를 내놨다(시험_릴스화면이
잡음). 후보 mime 마다 0.5초 시험 녹화를 돌려 데이터가 실제로 나오는 쪽을 고르는
probeMime() 으로 교체 + 0바이트 마지막 그물. 고친 뒤 헤드리스에서도 진짜 mp4
(202KB)가 out\ 까지 갔다.

### 시험
- 시험_릴스.py 신설 17개(서버: reel.html 마커·저장 바이트 온전·이름 밀기·거절·
  포트 겹침 회귀). 🔴 처음에 URL 에 한글을 그대로 넣어 ascii 인코딩으로 죽었다
  (quote 로 수정). 포트도 7897→7887(+1 이 스타일모드 7898 과 겹쳐서).
- 시험_릴스화면.py 신설 9개(헤드리스 CDP 9347: 담기→녹화→blob→저장→파일 대조,
  시험이 만든 것만 치움).
- 회귀 전부 통과: 주제찾기 53(pure 서버 포트를 7885 로 - 7894 가 경제 시험과
  겹쳐 **고친 코드가 비켜 가는 바람에** 엉뚱한 서버를 두들겼다) / 스타일모드·
  링크이어넣기 37 / 경제 차트후보 23.

### 산출물 (셋 다 확인)
- exe 재빌드 + pure 복사(00:58) - 본판·pure 동시 기동 끝단 확인(7870 false/7871 true).
- 「(앱).zip」·「(pure)(앱).zip」 각 346항목·29.9MB(+1 = reel.html, 역슬래시 0,
  ★txt 구 ZIP 승계, 사용설명서는 릴스 단락 추가판). reel.html 은 probeMime 수정판
  으로 번들 재동기화(해시 일치 확인).
- 바탕화면 .lnk 4개 대상 존재 확인.

## 2026-08-14 (2차) - 갤럭시(폰)에서 작업할 수 있게

사용자 요청 "뉴보대 앱으로 갤럭시에서 작업할 수 있게". 접속은 둘 다(같은 와이파이 +
Tailscale), 화면은 전 화면 손질로 확정받고 진행.

### 만든 것
- **모바일 1열 접기**: style.css 에 `@media (max-width:820px)` - body 스크롤 허용,
  3단(.panel/.stage/.panel.right)을 위→아래 1열로, 캔버스는 폭 기준(min(100%,430px)).
  reel(3단 격자→1열)·mark(비교 3칸→auto-fit)·topics(검색칸 고정폭 해제)는 각자 파일에.
  PC 는 그대로(pywebview 창 최소폭 1100 > 820 이라 안 겹침). 캔버스 드래그는 이미
  pointer 이벤트라 터치가 그냥 된다.
- **접속 열기**: app.py 에 `_reach_addrs()` - `--host 0.0.0.0` 이면 시작 배너에
  "폰 : http://192.168...:7870/ (같은 와이파이에서) / http://100...:7870/ (Tailscale)" 를
  찍어 준다. 창은 0.0.0.0 이 아니라 127.0.0.1 로 연다(0.0.0.0 은 바인딩 표기).
- **「갤럭시에서 작업하기.bat」 3곳**: 뉴보대 루트(소스, --host 0.0.0.0 --no-browser)와
  본판·pure exe 옆(ipconfig 로 IPv4 를 보여 주고 `start /wait exe --host 0.0.0.0`).
  바탕화면에 「뉴보대 갤럭시에서 작업하기.lnk」 신설.
- **방화벽 규칙 「카드뉴스 메이커 (폰 접속)」**: TCP 7870-7889 인바운드 허용을
  **사설망(192.168/10/172.16)+Tailscale(100.64.0.0/10) 대역으로 한정**. 카페 공용망
  같은 데서는 막힌다. 경제 메이커 포트(7880대)도 이 규칙이 덮는다.
- 사용설명서에 「갤럭시(폰)에서 작업하기」 단락.

### 검증
- 9개 화면 전부 412px 기기 에뮬레이션(CDP)으로 가로 넘침 0 실측. 처음 스크린샷에서
  단추가 잘려 보인 건 **헤드리스 창 최소폭(~500px) 레이아웃을 412px 로 잘라 찍은 착시**
  - Emulation.setDeviceMetricsOverride 로 다시 재니 멀쩡했다. 눈보다 숫자.
- 0.0.0.0 으로 띄워 와이파이 IP·Tailscale IP 양쪽 200 응답 확인.
- 시험 8종 전부 통과(인스타/인스타화면/주제찾기53/릴스17/링크이어넣기37/주제흐름22/
  릴스화면/스타일모드32).

### 🔴 헤드리스 시험 플레이크 3층 - 하루 종일 이것과 싸움
스타일모드 시험이 같은 코드로 통과/실패를 오갔다(7실패→0→1…). CSS 탓인지 대조
실험(블록 떼고 붙이고)까지 했는데 **전부 재는 쪽 문제**였다. 층이 셋:
1. **배경 탭 타이머 얼기**: about:blank 탭이 앞에 남으면 앱 탭의 setTimeout·폰트
   로드가 언다(동기 평가만 됨) → 저장 디바운스(400ms)가 영영 안 적힘.
   `Page.bringToFront` + 스로틀 금지 플래그 3종을 시험 4종에 추가.
2. **프로필 잠금 경합**: 연속 실행 시 지난 크롬이 잠금을 놓기 전에 같은 프로필로
   또 뜨면 저장소가 임시 모드로 떨어짐 → 프로필을 tempfile.mkdtemp 로 회차마다 새로.
3. **내비게이션 직전 쓰기 유실**: localStorage 에 적힌 것을 **확인하고** 넘어가도
   새 문맥이 옛 값을 보는 일이 드물게 있다(8회 중 1회꼴) → 복원이 안 보이면 새
   문맥에서 같은 상태를 다시 적고 한 번 더 새로고침(진짜 boot 고장만 실패하게).
   고정 sleep 은 전부 조건 대기로 바꿈. 8연속 통과 확인.
- 교훈: **플레이크는 "다시 돌리면 통과"로 덮지 말고 층을 가를 것.** 진단 메시지
  ("디바운스가 얼어 직접 흘림" 등)를 시험 출력에 남겨 다음에 또 오판하지 않게 했다.

### 산출물
- exe 2종 재빌드(본판 빌드 후 pure 복사) + 「(앱).zip」 2종 갱신(해시 대조, 갤럭시 bat
  추가) + 바탕화면 .lnk 5개(기존 4 + 갤럭시 1) 대상 확인.

## 2026-08-14 (3차) - 릴스 자동 제작·편집

사용자 요청 "pure 판·갤럭시 작업도 릴스 자동 제작·편집이 되게". 확인해 보니 릴스
화면 자체는 이미 세 배포 모두에 있었다(pure exe 번들에 reel.html 동봉, 갤럭시 bat 은
소스 직행) - 없던 것은 "자동"과 "편집"이라 기능을 올리고 셋에 다시 실었다.

### 만든 것 (전부 reel.html 안, 서버는 무변경)
- **⚡ 최근 카드 자동 담기**: ① 인스타 올리기로 담아 둔 임시 카드 ② 없으면 out 의
  최근 세트(제일 새 그림과 10분 안에 같이 저장된 것)를 이름순(01_표지 먼저)으로 담는다.
- **⚡ 자동 제작**: 단추 하나로 담기→녹화→out 자동 저장까지. 그림·흐린 바탕이 다
  구워질 때까지 기다렸다가 녹화한다(20초 한도).
- **편집**: 장별 시간 칸(1~8초, 비우면 기본값) + 미리보기 훑기 슬라이더(녹화 중엔
  숨김 - 미리보기 캔버스가 녹화 화면이라 섞이면 안 됨) + 「← 편집으로 돌아가기」
  (영상은 그대로 두고 화면만 되돌림, 저장 단추 유지).

### 🔴 probeMime 플레이크 한 층 더 - 인코더 예열
시험_릴스화면 첫 판에서 probeMime 이 후보 전부 0바이트로 끝나 "녹화 못 함"이 났다
(재실행은 통과). 막 뜬 창은 MediaRecorder 인코더 예열이 늦어 0.5초 창구가 통째로
빌 수 있다 → 첫 판이 전부 비면 1.2초로 한 번 더 도는 2단으로 고쳤다. 이후 연속 통과
(헤드리스에서 mp4 가 잡히는 판도, webm 으로 물러서는 판도 있었다 - 둘 다 정상).

### 시험
- 시험_릴스.py 22개(새 마커 5개 추가) / 시험_릴스화면.py 12개(자동 담기 + 자동 제작
  끝단: 단추 하나로 녹화→자동 저장→out 파일까지 실측) / 링크이어넣기 37 회귀 통과.

### 산출물 (해시 대조 후 다른 것만 교체)
- 앱 2곳 `_내부\static\reel.html` 교체(소스와 해시 일치 확인).
- 「(앱).zip」·「(pure)(앱).zip」 각 347항목 유지, reel.html·사용설명서.md 2건씩 교체
  (교체 후 ZIP 안 해시 재검증). exe 재빌드는 불필요 - reel.html 은 데이터 번들이라
  exe 바이너리가 안 변한다.
- 바탕화면 .lnk 대상 경로 무변경(pure·갤럭시·본판 모두 그대로 유효).

## 2026-08-14 (4차) - 젯슨 상시 서버 + 관련 숏폼 연동

사용자 요청 둘: ① "갤럭시 작업이 노트북이 꺼져 있어도 되게" ② "릴스는 관련 동영상을
인스타·유튜브에서 숏폼 형식으로 따오는 것을 연동".

### ① 젯슨 상시 서버 (노트북 없이 갤럭시 작업)
- 팀 젯슨(100.98.39.102, Tailscale SSH, syntax_kmu)에 newsfeed 소스를 올리고
  `--host 0.0.0.0 --no-browser --port 7870` 으로 상주시켰다. 폰은 Tailscale 만 켜면
  `http://100.98.39.102:7870/` 로 어디서든 접속.
- 지킴이 = crontab `*/5 * * * * ~/newsfeed/watchdog.sh` - **살았는지는 pgrep 이 아니라
  포트 응답(curl 127.0.0.1:7870)으로 잰다.** 🔴 pgrep -f "python3 app.py" 는 ssh 로
  부른 셸 명령줄에 같은 글자가 있으면 그걸 잡아 "살아 있다"고 착각했다(실측 - 첫
  배포에서 서버가 안 떠 있는데 지킴이가 침묵). sudo 없이(비번 파이핑이 권한 분류기에
  막히는 젯슨 특성) cron 만으로 재부팅 생존까지 해결.
- 한계: 인스타 자동 올리기(크롬 CDP)는 PC 전용. 저장은 젯슨 out\ 에 남으므로 폰
  흐름은 「이 기기에 내려받기」로.
- 갤럭시 bat 3곳(루트+본판+pure)에 젯슨 주소 안내 줄 추가(cp949 로 읽고 써야 함).

### ② 관련 숏폼 연동 (릴스 화면)
- `/api/shorts` 신설: 유튜브 검색 페이지의 ytInitialData 를 키·설치 0개로 파싱,
  4분 미만만(sp=EgIYAQ%3D%3D + 초 검사 보강), videoRenderer+reelItemRenderer 줍기,
  검색어별 10분 캐시. 검색어 없이 url 만 오면 extractor 로 기사 제목을 뽑아 쓴다.
- reel.html 「관련 숏폼 참고」: 썸네일(i.ytimg.com) 격자 → 누르면 youtube-nocookie
  embed 세로 모달로 재생. 인스타는 밖에서 검색을 막아 검색 링크만 새 탭.
- **내려받지 않는다**(남의 영상을 릴스에 붙이면 저작권 문제) - 벤치마킹용임을 화면에 명시.
- 「이 기기에 내려받기」 단추 추가: 만든 릴스 blob 을 a[download] 로 - 폰 갤러리로
  바로 가서 인스타 앱 업로드로 이어진다.

### 시험·산출물
- 시험_릴스.py 33개(shorts 실검색 5개 포함) / 시험_릴스화면.py 12개 / 전부 통과.
- app.py 가 바뀌어 이번엔 **exe 재빌드 필수**(3차와 달리 서버 코드) - 본판 빌드 후
  pure 복사, 양쪽 스모크(pure:true/false 확인). ZIP 2종은 항목 교체가 아니라 **앱
  폴더 기준 재구성**(재빌드로 _내부 구성이 달라질 수 있어서) + ★txt·설명서 승계,
  347항목 유지, reel/exe/설명서 해시 검증.
- 젯슨에 reel.html·설명서 재동기화(정적 파일은 서버 재시작 불요, app.py 를 바꾸면
  pkill 후 watchdog 이 새 코드로 띄움).

## 2026-08-15 (5차) - Cloudflare Tunnel 공개 주소 + 열쇠 문

사용자 결정 "cloudflare tunnel으로 해보자" + "tailscale은 젯슨 연결용으로만" +
크롬/깃허브 사용 허락. 폰에 Tailscale 없이 어디서나 접속되는 공개 주소를 열었다.

### 만든 것
- **열쇠 문(--key)**: 앱은 로그인이 없고 /api/proxy·저장 API 가 있어 공개 주소에
  열쇠 없이 내걸면 안 된다 → app.py 에 `--key` 신설(기본 꺼짐 - PC·exe 불변).
  ?key=열쇠 가 맞으면 쿠키(nbkey, 1년)를 심고 key 를 뗀 주소로 303, 이후 쿠키로
  통과, 없으면 GET·POST 전부 401. 🔴 열쇠는 영문·숫자만(한글이면 URL ascii 인코딩
  과 Set-Cookie 헤더 양쪽에서 탈난다). 시험_열쇠.py 11개 신설, 릴스 33개 회귀 통과.
- **젯슨 터널**: cloudflared(arm64, sudo 불요) + watchdog.sh 확장(앱은 포트 응답,
  터널은 metrics /ready 로 판정, /quicktunnel 에서 지금 주소를 tunnel_url.txt 로).
  열쇠는 ~/newsfeed/.key(600). quick tunnel 이라 **주소가 재시작마다 바뀐다**.
- 끝단 실측: 공개 주소로 열쇠 없이 401 → ?key= 303+쿠키 → 릴스 화면·insta-files·
  shorts(18건) 전부 200.

### 🔴 함정 셋
1. **pkill/pgrep 자기잡기 재발**: `pkill -f 'no-browser'` 가 ssh 명령줄의 같은
   글자를 잡아 **ssh 세션 자체를 죽였다**(exit 255). 대괄호 패턴(`app[.]py`)이나
   `pkill -x` 로 자기 일치를 피할 것. watchdog 의 pgrep 함정과 같은 뿌리.
2. **QUIC 막힘**: 젯슨 망이 UDP 7844 를 막아 기본 프로토콜로는 엣지에 못 붙었다
   (530/1033 + "failed to dial quic" 무한 재시도). `--protocol http2` 로 강제.
3. **터널 주소 파일이 한 박자 늦는다**: 재시작 직후 /quicktunnel 을 바로 읽으면
   옛 주소가 나올 수 있다 - 판단은 tunnel.log 의 마지막 등록 줄이 정답.

### Cloudflare 계정 확인 (크롬)
- water.hyun.p@gmail.com 계정 로그인 상태 확인, **도메인 0개** → 고정 주소(named
  tunnel + Access)는 도메인이 생겨야 가능. 구매는 사용자 몫이라 보류.

### 산출물
- exe 재빌드(본판+pure) + ZIP 2종 재구성(347항목) + 갤럭시 bat 3곳(공개 주소
  바로가기 안내로 교체) + 바탕화면 「뉴보대 공개 주소 (폰 어디서나).url」(열쇠 포함)
  + 사용설명서 갱신 + 젯슨 동기화(app.py·watchdog·설명서·시험_열쇠).

## 2026-08-16 (6차) - 위/아래 띠(BREAKING NEWS)·글자별 조절·브랜드 토큰(brands.js) 마무리

지난 밤(08-15 21시대) 세션이 design.md 21종·브랜드_동기화.py·brands.js·띠 패널·
시험_띠.py 까지 짜다가 끊겼다. 이번 세션은 "무엇이 되고 안 됐는지"부터 재고 빈 곳만 채웠다.

### 이미 되어 있던 것 (코드 손 안 댐)
- app.js: 위·아래 띠 상태(`S.bands`)·그리기(반복 문구 ` · ` 이어 붙이기)·패널
  (buildBandPanel: 켜기/반복/문구/색/글자색/높이/서체/굵기/크기/정렬/투명도)·프리셋 4개·
  저장·복원·옛 상태 마이그레이션·deco band 겹침 회피·색 테마→띠 색만 교체.
- 글자 4층(kicker/제목/본문/credit) 서체·굵기·크기·색·강조색·줄간격·자간·투명도·줄폭·
  정렬·그림자·뒷배경 조절 + 서체별 가능 굵기만 보이는 select(가짜 볼드 방지).
- brands.js 병합(mergeBrandStyles: 같은 브랜드는 내장 위에 덮고 새 브랜드는 select 로),
  applyStyle 이 띠까지 입힘, pure 판은 브랜드 select 숨김.

### 이번에 한 것
- **시험_띠.py 가 첫 항목에서 죽던 원인 = 시험 쪽 대기 함정 2개**(앱 버그 아님):
  1. `navigate` 3초 고정 대기가 첫 로드(큰 글꼴 CSS)에 모자라 `STYLES is not defined`.
     → `wait_js` 가 ReferenceError 동안은 되묻고, `goto()` 가 app.js 준비를 기다림.
  2. 그렇게 고치니 이번엔 **옛 페이지의 STYLES 를 보고 "준비됨"으로 오판** —
     navigate 직후 새 문서로 넘어가기 전에 검사가 통과해 pure 판 항목이 본판 값을 읽었다.
     → 옛 페이지에 `window.__nb_old_doc` 표식을 남기고 그게 사라진 뒤에만 준비 검사.
  3. pure 서버를 `port+1` 고정으로 띄우는데 그 포트에 지난 시험 잔재가 있으면 app.py 가
     조용히 다음 포트로 옮겨 **엉뚱한 서버의 config 를 읽는다** → 빈 포트를 먼저 잡게.
- 시험_띠.py 에 [13] 글자별 조절이 캔버스에 닿는가(색 픽셀 수·크기 배율·정렬 좌우·
  서체·층 전환) 6항목 추가 → **55/55 통과**.
- 시험_스타일모드.py: 어피티 토큰이 위 띠(MONEY LETTER)를 켜서 "위쪽이 밝다" 픽셀 검사가
  띠 색을 읽었다 → 띠 높이 아래를 재게 고침(32/32). 주제찾기 53/53 회귀 통과.
- brands.js 재생성(6→ design.md 전부, 이번 세션 기준 17개+). 사용설명서 §4 디자인 갱신.

### 🔴 남는 함정
- 시험 중 서버 Popen 이 살아남아 포트를 붙잡을 수 있다(7892 에 지난 probe 서버가 남아
  pure 항목이 헛돌았다). 시험 전 `netstat -ano | findstr :789` 로 잔재 확인.
- 시험_인스타화면.py·시험_릴스화면.py 는 10분 넘게 걸려 이번엔 완주 못 봄(이번 변경과
  무관한 화면 - insta/reel 은 app.js 를 안 쓴다).

### 02:10 exe 완전 재빌드 (사용자 "뉴보대는 안 열림, 응용프로그램까지 다같이 업데이트")
- 앞서는 static 4파일만 갈아끼웠는데(app.py 무변경) 사용자가 안 열린다고 해 PyInstaller 로
  본판 새로 굽고(6,794,876B, 02:10) pure 복사. 갤럭시 bat 승계, 번들 static 5파일 해시 = 소스.
- 스모크: 본판 창 뜸·7870·config pure:false·brands.js 200(21개) / pure 창 뜸·pure:true → 종료.
- ZIP 2종 앱 폴더 기준 재구성(348항목·29.9MB, ★txt 구 ZIP 승계, 설명서 최신, exe·app.js 해시
  일치, 역슬래시 0). 🔴 파이썬으로 이름 조립할 때 "(앱)" 앞 띄어쓰기 빠져 파일이 하나 더 생겼다
  → 이름 바로잡음. 젯슨은 app.py·static 해시 동일이라 손대지 않음.
- 재현 실패 기록: 재빌드 전 exe 도 내 쪽에선 창이 뜨고 사용자 실제 WebView2 프로필(창데이터)
  로 헤드리스 렌더도 정상. 원인 후보 = ① 바탕화면에 본판 lnk 가 없어(pure 만) 헷갈림 → 본판
  lnk 재생성됨 ② 시험 잔재 서버가 7870 을 잡고 있어 exe 가 7871 로 갔는데 창은 그 주소를 여니
  문제 없음 - 다만 열린 Edge 창("뉴보대 카드뉴스 메이커 외 페이지 2개")이 옛 주소를 봤을 수 있음.


## 2026-08-18 (7차) — 띠 문구를 왼쪽 「2. 문구 고르기」로 끌어옴 + 바탕화면 공개 주소 HTML

사용자 지적 두 가지에서 출발했다. ① 바탕화면 `.url` 이 폰에서 안 열린다 ② `어피티풍`
을 누르면 위에 뜨는 「MONEY LETTER」 를 제목처럼 왼쪽에서 고치고 싶다.

### 만든 것
- **왼쪽 띠 문구 칸** (`index.html` + `app.js` + `style.css`)
  - 「2. 문구 고르기」 안에 **위 띠 문구**(후킹 문구 위) · **아래 띠 문구**(출처 아래)
    두 칸 신설. 카드에 그려지는 차례(위 띠 → 후킹 → 제목 → 요약 → 출처 → 아래 띠)와
    패널 차례를 맞췄다.
  - 오른쪽 「4. 디자인 → 위/아래 띠」 와 **같은 `S.bands[k].text` 를 본다** — 어느 쪽을
    고쳐도 반대쪽 칸·켜기 체크가 따라온다(`syncBandTexts()` 를 `syncBandControls()`
    맨 앞에서 부르므로 스타일 모드·브랜드·프리셋·상태복원 길이 전부 이미 지나간다).
  - 각 칸의 `표시/숨김` 미니 단추 = 그 띠의 on. 다른 문구 칸들과 생김새를 맞췄다.
  - **꺼진 띠에 글자를 치면 저절로 켜 준다.** 안 그러면 쳐도 화면이 그대로라 답답하다
    (사용자가 겪은 것이 정확히 그 답답함이다).
- **바탕화면 `뉴보대 공개 주소 (폰 어디서나).html`** — `.url` 은 윈도우 전용 포맷이라
  폰이 못 읽는다. 열기 단추·주소 복사·살았는지 표시·새 주소 붙여넣기 칸을 담았다.

### 확인
- 시험_띠.py **55항목 전부 통과**(회귀 없음). node --check 로 app.js 문법 확인.
- 크롬에서 실제로 걸어 봄: 어피티풍 → 왼쪽 칸에 MONEY LETTER 들어옴 → 고치면 캔버스
  바뀜 → 오른쪽 칸도 같이 바뀜 → 꺼진 아래 띠에 글자 치니 자동 켜짐 → 새로고침해도 남음.
- 산출물: exe 폴더 static 3파일 교체(app.py 무변경이라 재빌드 불요, 해시 대조 ok) +
  ZIP 2종은 **348항목 그대로 두고 3항목만 갈아끼움**(나머지 345개는 바이트 그대로 —
  배포 전용 파일이 섞여 있어 통째 재구성보다 안전) + 젯슨 static 동기화(해시 일치).
- 공개 주소로 끝단 확인: index 200 에 왼쪽 칸 2개, app.js 200 에 syncBandTexts 7군데.

### 🔴 함정
- **한글 사용자 폴더 때문에 `ssh`/`scp` 가 known_hosts 를 못 읽는다.** 경로가 cp949 로
  깨져(`/c/Users/¹ÚÇö¼ö/.ssh/known_hosts`) "Host key verification
  failed" 가 난다 — 같은 세션에서 되다 안 되다 한다. `-o UserKnownHostsFile=<ASCII 경로>`
  로 우회할 것. statusline·hwp 때의 cp949 함정과 같은 뿌리.
- 크롬 확장의 screenshot 이 이 세션 내내 `params.clip.scale` 오류로 죽었다. 그림 대신
  `javascript_tool` 로 상태·캔버스 dataURL 을 재서 확인했다(오히려 증거가 셌다).

### 미결
- quick tunnel 주소가 재부팅마다 바뀌는 문제는 그대로다. 젯슨에 **Tailscale Funnel**
  (1.102.2, CertDomains `jetson.taild63ab0.ts.net` 이미 발급됨)을 켜면 고정 주소가
  공짜로 생긴다 — 공개 범위를 바꾸는 일이라 사용자 승인 대기.

---

## 2026-08-20 — 폰 주소 404 를 잡다 (사이트는 이미 살아 있었다)

### 무슨 일이었나
바탕화면 `뉴보대 공개 주소 (폰 어디서나).html` 의 「지금 열기」가 404. 새 사이트를
만들 일인 줄 알았는데 **아니었다** — 깃허브 페이지는 이미 `status: built` 로
멀쩡히 돌고 있었다(`main` 브랜치 `/docs`, https 강제).

박아 둔 주소가 두 군데 틀렸다.

    적혀 있던 것  https://park-hyun-su.github.io/new_bo_dae/   ← 404
    실제          https://koreauniversityforum.github.io/new_bo_dea/  ← 200

1. **계정이 다르다** — `park-hyun-su` 가 아니라 `koreauniversityforum`
2. **철자가 다르다** — `new_bo_dae` 가 아니라 `new_bo_dea`

HTML 을 만든 시각이 15:38, 커스텀 도메인을 풀어 주소를 되살린 커밋(0763bc7)이
17:54 다. **주소가 확정되기 전에 안내 페이지를 먼저 구워 놓고 갱신을 잊은 것**이다.

### 고친 것
- 바탕화면 HTML — 주소 3곳 + **QR 이미지 자체**를 새 주소로 갈아끼움.
  QR 은 `qrcode` 로 굽고 `cv2.QRCodeDetector` 로 되읽어 원문과 일치하는지 확인했다
  (눈으로는 QR 이 맞는지 절대 못 본다 — 옛 QR 도 「그럴듯하게」 생겼었다).
- `README.md` 맨 앞에 **폰에서 여는 주소** 절 신설. 주소가 프로젝트 안 어디에도
  없었다(`grep` 결과 0건) — 바탕화면 HTML 한 장에만 살아 있었으니 그게 틀어지면
  아무도 못 찾는 구조였다.

### 확인
- 폰판 화면 5장이 참조하는 파일 **22개를 전부 실제로 받아 봄**. 404 는 5개인데
  전부 예상된 것: `${thumbUrl(...)}` 은 JS 템플릿 문자열이고, `feed/insta/out/topics
  .html` 은 `폰shim.js:334` 의 `HIDE` 가 폰에서 아예 지우는 PC 앱 전용 화면이다.
- 한글 파일명(`폰shim.js`·`assets/뉴보대_로고.png`)은 **퍼센트 인코딩해야 200** 이
  나온다. 날것으로 curl 하면 404 라 「빠졌다」고 오판하기 쉽다.

### 🔴 함정
- **터널을 되살릴 필요가 없었다.** 작업일지 미결의 「재부팅마다 주소가 바뀐다」에
  끌려가면 Tailscale Funnel 부터 켜게 되는데, 폰판은 서버가 0개라 깃허브 페이지로
  이미 24시간 열려 있다. 젯슨 터널은 **PC 앱 기능(URL 가져오기·릴스 자동·out 폴더)**
  이 필요할 때만 쓰는 물건이다.

### 미결
- `docs/읽어보세요.txt`(굽는 곳: `newsfeed/폰판_만들기.py:163`)가 릴스를 「안 되는
  것」으로 적고 있는데 `reel.html` 은 실제로 올라가 있다. 수동 녹화는 되고 「최근
  세트 자동 담기」만 서버가 필요한 듯한데 폰에서 실제로 눌러 확인하지 못했다.

---

## 2026-08-20 (2차) — 폰의 「Failed to fetch」: 대리인 한 명을 세우다

### 무엇이었나
폰에서 기사 주소를 넣고 「가져오기」를 누르면 `Failed to fetch`. 헤드리스 크롬에
**갤럭시 UA + 412×915** 를 씌워 실제 사이트를 걸어 재현했다.

    폰shim.js › fetchArticle()
      await __nbdFetch(url, { mode: 'cors' })   ← 언론사 서버를 **직접** 부른다

언론사가 `Access-Control-Allow-Origin` 을 줄 리 없으니 이 길은 **100% 실패**다.
주석에는 "열려 있으면 덤으로 시도한다"고 적혀 있었지만, 실제로는 덤이 아니라
**언제나 실패하는 길**이었다. 화면에는 단추가 그대로 남아 있어 사용자는 매번 눌렀다.

### 재 본 것 — 대리인 후보 3
| | 결과 |
|---|---|
| `api.allorigins.win` | 500 (CORS 헤더도 없음) |
| `api.codetabs.com` | 522 |
| **`r.jina.ai`** | **200 · CORS 우리 주소 허용 · 1.4초** |

`x-return-format: html` 을 주면 **렌더링된 HTML** 이 그대로 온다 — `og:title` 도
`#dic_area`(네이버 본문 칸)도 살아 있어서 **아래 파싱 코드를 한 줄도 고치지 않았다.**
사용자 지정 헤더라 프리플라이트(OPTIONS)가 뜨는데, 그쪽이 `x-return-format` 을
허용하고 `access-control-max-age: 25200`(7시간)까지 준다.

### 고친 것 (`newsfeed/폰판_소스/폰shim.js` → `폰판_만들기.py` 로 다시 구움)
- `READER = 'https://r.jina.ai/'` 를 거치게. 429(분당 20회 초과)는 「1분 뒤에」로,
  연결 실패는 「인터넷 확인」으로 풀어서 말한다. **날 오류를 그대로 보여주지 않는다.**
- 출처·날짜 대체 경로. 대리인이 준 HTML 에는 `og:site_name` 과
  `article:published_time` 이 **없다**(네이버 실측). 출처 칸이 비면 카드 아래가
  휑해지므로 `twitter:creator` → `og:article:author` 앞토막, 날짜는 `[data-date-time]`
  으로 찾는다. 이걸 넣기 전 실측은 언론사 `""`, 넣은 뒤 `헤럴드경제`·`서울경제`.
- 문서 셋을 사실에 맞춤 — `README.md`, `읽어보세요.txt`(굽는 곳 `폰판_만들기.py`),
  바탕화면 안내 HTML. 셋 다 「폰은 URL 을 못 읽는다」고 적고 있었다.

### 시험 — `newsfeed/시험_폰_기사가져오기.py` 신설 (28항목 전부 통과)
남의 서비스에 기대는 자리라 **조용히 죽는다.** 그래서 언론사 4곳을 실제로 건다.

    네이버 2109자·문화일보 / 연합뉴스 756자 / 한겨레 1005자 / 경향신문 3247자
    넷 다 제목·사진·출처·문구 후보까지 나옴

🔴 기사 주소를 시험에 **박아 두지 않았다** — 며칠이면 죽는다. 목록 화면에서 그때그때
집는다. 마지막 항목은 "대리인이 죽어도 **본문 붙여넣기**는 살아 있는가" 를 잰다.

### 🔴 함정
- **`S`·`STYLES` 가 `window` 에 없다.** 폰에서 `typeof window.S === 'undefined'` 를 보고
  "app.js 가 안 실렸다"고 오판할 뻔했다. `let` 선언은 window 에 안 붙는다(스크립트 스코프).
  기존 시험들이 맨이름 `S` 로 쓰는 이유다. 캔버스 1080×1350·단추 47개로 멀쩡함을 확인.
- **가짜 URL 로 시험하면 안 된다.** 없는 기사 번호를 넣었더니 네이버 오류 페이지(341자)를
  기사로 읽고 `ok:true` 가 나왔다. 지금도 그렇다 — 오류 페이지를 걸러내지는 못한다.
- 한글 파일명은 **퍼센트 인코딩해야 200**(`폰shim.js`·`assets/뉴보대_로고.png`).

### 미결
- 오류 페이지·로그인 담벼락을 기사로 오인하는 문제(본문 길이만으로는 못 가린다).
- 대리인 의존. 끊기면 붙여넣기로 내려앉지만 자동 감지는 없다. 우리 Cloudflare Worker
  로 옮기면 남에게 안 맡기게 되는데, CF 로그인이 대화형이라 사용자 몫이다.

---

## 2026-08-20 (3차) — 사진도 대리인을 거치게

### 무엇이었나
「가져오기」가 되기 시작하니 다음이 사진이었다. 세 갈래를 나눠 실측했다.

| 사진 출처 | CORS 헤더 | 폰에서 |
|---|---|---|
| 기사 딸림 사진 `imgnews.pstatic.net` | **없음** | ❌ 안 뜸 |
| 위키미디어 공용 `upload.wikimedia.org` | `*` | ✅ |
| Openverse `api.openverse.org` | `*` | ✅ |

막힌 건 **기사 사진 하나**였다. `폰판_만들기.py` 가 `loadImage()` 에
`crossOrigin='anonymous'` 를 붙이는데(캔버스 오염 = 저장 실패를 막으려고),
언론사 사진 서버가 CORS 를 안 주니 브라우저가 **아예 거부**한다.
crossOrigin 을 떼면 보이기는 하나 `toDataURL` 이 통째로 막히므로 뗄 수 없다.

### 고친 것
`폰판_만들기.py` 의 `proxy()` 교체문을 `PHONE_PROXY` 상수로 빼고, CORS 를 붙여 주는
`wsrv.nl` 을 거치게 했다(실측 200 · `CORS=*` · 원본 그대로의 PNG 86KB).

🔴 **감쌀 곳만 감싼다.** 위키미디어·Openverse·`data:`·`blob:`·이미 감싼 주소는
   그대로 둔다. 전부 대리인에게 맡기면 지금 잘 되는 사진 검색까지 같이 죽는다.
   두 번 감싸는 것도 막았다(app.js 1090줄 주석이 경고하던 그 자리).

### 확인 — 시험 37항목(사진 9항목 추가) 전부 통과
- `proxy()` 가 6가지 주소를 각각 감싸는지/두는지
- 기사 사진을 실제로 **캔버스에 얹고 `toDataURL` 까지** — 800×533 성공.
  🔴 그려지는 것만 보면 모른다. 오염은 **저장할 때** 터진다. 그래서 저장까지 잰다.
- 위키미디어 30장도 캔버스+저장 성공, 그리고 **대리인을 안 거치는지**까지 확인

### 미결
- 대리인이 둘(`r.jina.ai` 글 / `wsrv.nl` 사진). 둘 다 남의 무료 서비스다.
  하나로 합치려면 우리 Cloudflare Worker 인데 CF 로그인이 대화형이라 사용자 몫.

---

## 2026-08-23 - 시리즈(캐러셀) 편집기 · AI 문구 · 내 프리셋 · 미결 정리

사용자 질문은 「바탕화면 뉴보대 카드뉴스 메이커, 조금 더 발전시킬 수 있을까」였다.
실제로 띄워 보고 찾은 빈 곳을 네 가지로 제안했고 네 가지 다 고르셨다.

### 무엇이 비어 있었나
- 기능 폭은 넓은데 구조가 **「기사 1건 = 앞장 1장」**이었다. 캐러셀은 앞장을 만들고 → 올리기에
  담고 → 돌아와 같은 틀로 2장째 … 왕복이었다. 첫 게시물(`첫게시물\`)은 5장이었고 `out\` 에는
  저장된 카드가 없었다 - 흐름이 실사용과 안 맞는다는 신호로 읽었다.
- 문구는 전부 규칙기반(summarizer.py)이라 품질에 천장. AI 연동은 어디에도 없었다.
- 「내가 만든 디자인 조합」을 이름 붙여 두는 길이 없었다(스타일 모드·브랜드 토큰은 남의 결).

### 만든 것
1. **시리즈 편집기 `static/deck.js`**(새 파일) + `index.html` 띠 + `style.css`
   - 장마다 앱 상태 `S` 한 벌을 통째로 든다. 지금 장의 S·bgImg 는 app.js 전역 **그 자체**라
     편집기는 한 줄도 안 바꾸고 장만 갈아 끼운다(`commit()`/`activate()`).
   - 템플릿 7종: 포인트·숫자 강조·인용·목록·표지형·빈 장·**뒷장**(outro.js 로 그린다 -
     뒷장 장이 떠 있으면 app.js 의 render()·마우스·방향키는 손을 뗀다 `isOutroActive`).
   - 자동 구성 = `POST /api/series`(`summarizer.series()` 규칙기반 / AI 설정이 켜져 있으면
     `ai.run("series")`, **꼴이 같아 화면은 구별하지 않는다**).
   - 디자인 전 장 적용 / 시리즈 저장(`_01.._NN`) / PNG 전부 / 인스타 담기는 장이 둘 이상이면
     `NAV.mount({stage})` 가 `DECK.stageItems()` 를 준다(캐러셀 순서 그대로).
   - 저장은 localStorage `nb_deck`. 🔴 **한 장짜리는 복원하지 않는다** - 앱의 원래 저장
     `nb_state` 가 진실이고, 두 곳이 갈라지면 `시험_띠.py` 의 「옛 상태(bands 없음) 복원」이
     깨졌다(실제로 1건 실패해서 이렇게 정했다). 장이 둘 이상일 때만 장 목록이 진실.
2. **AI 문구 `ai.py`**(새 파일) + `/api/ai` + 왼쪽 「AI 설정」 + `✨ AI 문구` + 피드 글의
   `✨ AI 로 쓰기`. Claude API(urllib, 표준 라이브러리 - **설치 0개 규칙 때문에 SDK 대신**) /
   Ollama. 키는 화면(localStorage `nb_ai`)에만, 요청마다 실려 오고 서버 파일에 안 남긴다.
   `output_config.format = json_schema` 로 JSON 을 받고 `stop_reason: refusal` 을 따로 안내.
   기본 모델 claude-opus-5. 돌려주는 꼴 = 규칙기반과 동일(copy→analyze 꼴, series→series 꼴).
3. **내 프리셋** - `designOf(S)`(글·사진 뺀 전부) / `applyDesign()`. 프리셋과 「디자인 전 장
   적용」이 같은 함수를 쓴다. localStorage `nb_presets`.
4. **미결 정리** - ① 오류·로그인 페이지를 기사로 오인: `extractor.looks_like_error_page()`
   (제목·짧은 본문의 문구로 판단, 600자 넘는 본문은 안 건드림) + 폰shim 에 같은 규칙.
   ② `docs/읽어보세요.txt` 릴스 안내 고침(손 녹화는 된다). ③ 폰판에 `/api/series`(규칙기반,
   `summarizer.js` 에 `series()` 옮김 - 파이썬과 출력 일치 확인) / AI 단추는 폰판에서 숨김.

### 확인
- 새 시험 `시험_시리즈.py` **47항목 전부 통과**(서버 꼴 9 · 장 추가/전환 · 자동 구성 5장 ·
  stageItems · out 저장 5파일 · 새로고침 복원 · 순서/복제/지우기 · 프리셋 · AI 설정 · 새 기사).
- 폰판 `시험_폰_기사가져오기.py` 37항목 통과(재굽기 뒤).
- 회귀: `시험_띠.py` 55 → 1건 실패했다가 위 「한 장짜리는 복원 안 함」으로 해결(재실행 아래 기록).

### 🔴 함정 (이번에 실제로 겪은 것)
- **`hidden` 속성은 작성자 `display:flex` 에 진다.** `.ctl`·`.deck-menu` 처럼 display 를 준 요소가
  `hidden` 을 무시하고 계속 보였다(스크린샷에서 메뉴가 열린 채). `[hidden]{display:none!important}`
  를 style.css 에 넣었다.
- 헤드리스 시험에서 **새 문서마다 `window.confirm` 을 다시 덮어야** 한다. 새로고침 뒤 「새 기사」가
  confirm 을 띄워 페이지가 통째로 멈췄고(소켓 30초 타임아웃), 원인을 선택자 문제로 오진할 뻔했다.
- 시험 안에서 새로고침하면 **본문 칸이 비어** AI 단추가 서버까지 안 간다 - '먼저 기사를…' 안내를
  'AI 실패' 로 기대해 거짓 실패. 진단 출력(`fetchMsg` 실제 글)을 찍고서야 보였다.
- bash heredoc 안에 파이썬 패치를 넣을 때 `\n`·`\s` 이스케이프가 두 번 풀려 **replace 가 조용히
  빗나간다**(assert 없는 replace 는 "ok" 를 찍고도 아무것도 안 바꿈). 이후 Edit 도구로 직접 고쳤다.
- 규칙기반 시리즈의 소제목이 설명문 앞토막 '…' 이라 같은 말이 두 번 나왔다 → 소제목 = 그 문장
  제목투(44자), 설명 = **다음 문장**. 그래도 AI 판이 훨씬 낫다(안내문에 적어 둠).

### 산출물 (2026-08-23 16:0x)
- 회귀 재실행: `시험_띠.py` 55/55 · `시험_시리즈.py` 47/47 · `시험_스타일모드.py` 32/32 ·
  `시험_링크이어넣기.py` 37/37(1차에 [1]만 실패했다가 단독 재실행 통과 - 첫 가져오기가 내 편집과
  겹친 플레이크로 본다) · `시험_폰_기사가져오기.py` 37/37.
- exe 완전 재빌드(본판 6,808,604B, `--hidden-import ai` 추가) + pure 복사·개명, 갤럭시 bat 2곳 승계.
  스모크: 본판 7895 config pure:false·/api/series ok·/api/ai 도달·deck.js 200 / pure 7896 pure:true.
- ZIP 2종 앱 폴더 기준 재구성(각 348항목·29.9MB, 역슬래시 0, app.js·deck.js 해시 일치). 옛 ZIP 삭제.
- 바탕화면 `.lnk` 는 exe 경로 그대로라 손댈 것 없음.
- 폰판 재굽기 + 커밋 f633002 push(koreauniversityforum/new_bo_dea).
- 🔴 **젯슨 `~/newsfeed` 가 없다**(2026-08-21 업데이트 스냅샷만 있고 7870 응답 000). 폰 접속은 이미
  깃허브 페이지라 **젯슨 동기화는 생략** - 되살릴지는 사용자 판단(예전 5차 절차는 작업일지에 있다).

### 미결
- AI 문구는 실제 키로 끝단까지 돌려 보지 못했다(가짜 키 401 까지만). 사용자가 키를 넣고 한 번 눌러 봐야
  프롬프트·JSON 꼴·장 분할 품질이 검증된다. 응답 꼴이 틀리면 `ai.run()` 이 빈 칸으로 다듬어 주므로
  멈추진 않는다.
- 규칙기반 시리즈 소제목 품질은 여전히 거칠다(동사 noun 화 패턴 한계). AI 판 권장.
- 폰판에서 시리즈 「뒷장」 장은 OUTROST 의 로고(`/api/assets`)를 폰shim 이 주는 대로 그린다 - 실기기 확인
  은 못 했다(헤드리스는 기사 가져오기 37항목만).

## 2026-08-24 - 메타 공식 API 3창구(인스타·페이스북 페이지·스레드)

### 왜
계정이 둘(`news_univ`, `universityforum_korea`)이고 창구가 셋으로 늘었다. 크롬 조종
(`insta.py`)으로 이걸 다 하려면 화면 조종을 세 벌 만들어야 하고, 인스타 화면이 바뀔 때마다
셋이 같이 죽는다. 그래서 공식 API 경로를 **따로** 얹었다. `insta.py` 는 손대지 않았다 -
급할 때 쓰는 길을 없앨 이유가 없다.

### 고른 것과 이유
- 인스타를 「Instagram Login」이 아니라 **Facebook Login for Business** 로 붙였다.
  페이스북 페이지도 올려야 해서, 한 번의 동의로 IG 사용자 ID 와 페이지 토큰을 같이 받는 쪽이
  계정마다 브라우저 왕복을 하나 줄인다. 스레드는 어차피 앱 ID·시크릿부터 별개라 따로 간다.
- 앱은 **하나**. 계정은 앱의 **테스터**로 넣으면 심사 없이 게시까지 된다. 심사·비즈니스
  인증은 남의 계정에 올릴 때나 필요하다.
- 토큰은 `%LOCALAPPDATA%\뉴보대\메타_계정.json` - 앱 폴더 밖. 안에 두면 배포본 ZIP 을
  다시 구울 때 토큰이 통째로 딸려 나간다(크롬 프로필을 밖에 둔 것과 같은 이유).

### 함정 (겪은 순서)
- 🔴 **인스타·스레드는 그림을 바이트로 못 받는다.** `image_url` 로 공개 HTTPS 주소를 줘야
  하고 메타가 직접 긁어 간다. 그래서 `공개호스팅` 이 모듈의 절반이다. 세 방식(깃허브페이지·
  직접·터널)을 두고 골라 쓰게 했다. 기본은 이미 가진 `docs/` - 대신 배포 30초~2분을 기다리고
  깃 이력에 그림이 남는다는 값을 치른다.
- 🔴 **인스타는 JPEG 만 받는다.** 우리 카드는 캔버스에서 나온 PNG 다. 「설치 0개」 규칙이라
  Pillow 를 못 깔아 **윈도우 .NET(System.Drawing)** 을 파워셸로 한 번 부른다. 투명한 곳을
  흰색으로 먼저 깔지 않으면 JPEG 에서 **검게** 나온다.
- 🔴 컨테이너를 만들자마자 발행하면 실패한다. `status_code == FINISHED` 를 기다린다.
- 🔴 스레드 500자. 인스타 문구를 그대로 넘기면 대개 넘친다 - **자르지 않고 막는다**
  (조용히 자르면 문장이 잘린 게시물이 남는다). `--스레드문구` 를 따로 받는다.
- 🔴 돌아올 주소(redirect URI)는 HTTPS 여야 한다. 노트북에 https 를 세우는 건 배보다 배꼽이라
  이미 가진 깃허브 페이지를 돌아올 자리로 쓰고 주소창을 붙여넣게 했다. 스레드는 코드 끝에
  `#_` 를 달아 주므로 떼어내지 않으면 조용히 400 이 난다.
- 동의를 취소하면 `?error_reason=user_denied` 로 돌아오는데, 처음 판은 그 주소를 **코드로
  알고 보냈다**(시험이 잡았다). 지금은 먼저 막고 메타가 준 이유를 보여 준다.
- `폰판_만들기.py` 가 `docs/` 를 통째로 비우므로 `올림/` 은 남기도록 KEEP 을 뒀다. 안 그러면
  게시 중에 그림이 사라진다.

### 산출물
- 새 파일: `meta_api.py`(808줄) · `메타_연결.py` · `메타_올리기.py` · `메타_갱신.py` ·
  `시험_메타.py` · `폰판_소스/connect.html`
- `폰판_만들기.py`: EXTRA 에 connect.html 추가 + KEEP(`.git`, `올림`)
- `사용설명서.md` 끝에 「메타 공식 API 로 올리기」 절
- 시험 `시험_메타.py` **73/73 통과**(그물망 안 탐, PNG→JPEG 는 진짜로 변환해 확인).
  `--시늉` 끝에서 끝까지 스모크도 통과(1080×1350 두 장, 계정 3창구).
- 폰판 재빌드(화면 5·파일 41·13.3MB). 이때 `docs/deck.js` 가 같이 갱신됐다 - 8차의 한 장짜리
  deck 고침이 폰판에 **아직 안 실려 있었다**(이번에 딸려 들어감).

### 미결
- 🔴 **실제 계정으로 끝까지 못 돌려 봤다.** 앱 등록·테스터 수락·토큰 발급은 사람 손이 필요하다.
  첫 게시는 `--시늉` 으로 앞단을 본 뒤 한 계정·한 창구부터 해 볼 것.
- 스레드 앱 ID·시크릿이 페이스북 것과 다른지 대시보드에서 확인해야 한다(다르면 `메타_연결.py 앱`
  에서 따로 넣는다).
- 앱 화면(`인스타 올리기`)에는 아직 안 붙였다. CLI 로만 돈다 - 붙이면 exe 재빌드가 따라온다.


## 2026-08-24 (2차) - 폴더가 통째로 날아간 뒤 정리 + 추천 노래

### 시작은 바로가기 하나
사용자가 "바탕화면 `뉴보대 카드뉴스 메이커.lnk` 왜 이래?" 라고 물었다. `.lnk` 자체는
멀쩡했고 **가리키는 폴더가 통째로 없었다**. `한대포 임시` 안이 완전히 비어 있었고
휴지통에도 없었다(영구삭제). 사용자가 복구해 돌려놓았다.

사용자의 판단은 "관련 자료를 모두 깃허브에 올려두어 문제 없는 줄 알았다" 였다.
그게 어긋난 지점이 이번 정리의 핵심이다.

### 🔴 깃허브에 있다 ≠ 폴더를 지워도 된다
소스 166개 파일은 멀쩡했지만 `.gitignore` 가 **일부러 뺀 것**이 네 종류 있었다.

| 항목 | 왜 없나 | 되살릴 수 있나 |
|---|---|---|
| `뉴보대_제출DB.csv` | 제출자 이름·글이라 공개 저장소 금지 | **불가** |
| `newsfeed/out/` | 산출물 | 불가(과거 결과물) |
| `앱/` `*.zip` `*.exe` | 빌드물 | 재빌드 가능 |
| `build/*.spec` | `build/` 를 통째로 뺐다 | 불가했다 |

복구본에서 넷 다 살아 있는 것을 확인했다(제출DB 1,262B · out 14개 · 앱 2종 · spec).

### 고친 것 - .spec 을 깃에 남김
`build/` 로 막으면 안쪽 예외(`!build/*.spec`)가 **안 먹는다**. git 이 무시된 폴더를
아예 들여다보지 않기 때문이다. `build/*` 로 바꾸고 `!build/*.spec` 를 붙여 해결.
`git check-ignore -v` 는 부정 규칙에도 종료코드 0 을 주므로 믿지 말고
`git add -n` 으로 확인할 것 - 실제로 `.spec` 하나만 잡히고 `build.log` 는 빠졌다.

### 백업 배치
사용자 지시로 `뉴보대`·`homepage`·`카드뉴스 메이커` 를 E: 로 옮기고 C: 를 백업으로 남겼다.
robocopy 후 해시 대조 2,114개 전부 일치(node_modules·.next 는 개수·용량으로 갈음).
양쪽에 `_이 폴더는 백업입니다.md` / `_이 폴더가 원본입니다.md` 를 뒀고,
`백업하기.bat`(robocopy /MIR) 로 다음부터는 더블클릭 한 번이면 된다.

- 🔴 robocopy 종료코드 1 = "복사함" 이지 실패가 아니다. FAILED 열을 볼 것.
- 🔴 E: 는 외장이라 평소 빠져 있다. 실제로 작업 중에 뽑혀서 `Test-Path` 가 False 를
  내놓았고 잠깐 "파일이 없어졌나" 로 오판할 뻔했다. **드라이브 유무를 먼저 볼 것.**
- 바탕화면 `.lnk` 는 일부러 C: 쪽 exe 를 가리킨다(E: 없으면 안 열리므로).

### 만든 것 - 추천 노래 (`static/bgm.js`)
"인스타 올릴 때 추천 노래도 알려줬으면 함" 요청.

🔴 **곡 이름을 지어내지 않는 것**이 이 기능의 전부다. 앱에는 음원 목록도, 인스타
인기 오디오를 읽을 방법도 없다. AI 에게 물으면 없는 곡을 그럴듯하게 댄다.
그래서 **무드 + 인스타 오디오 탭에 그대로 칠 검색어**까지만 준다. 곡은 사람이 고른다.

- 무드 7종(차분·무거움·긴박·밝음·따뜻함·설명·미래), 규칙기반이라 통신 0.
- 제목에서 걸린 말은 3배. 갈래별 밑점수가 있어 단서가 없어도 뭔가 나온다.
- `heavy>=3` 이면 `bright` 를 0 으로 누른다 - **사고 기사에 신나는 곡을 추천하는 게
  이 기능에서 제일 크게 잘못될 수 있는 일**이라 규칙으로 막았다.
- 근거가 된 말을 함께 보여 준다(`근거: 숨져, 피해, 유가족`). 근거 없이 "이겁니다"
  하면 못 믿는다.
- 붙은 자리 셋: 만들기(저장하면 다시 잡음) · 릴스(카드 담을 때) · 인스타(캡션 바뀔 때).
  릴스 화면의 "🔇 소리는 넣지 않습니다" 안내 바로 아래가 제일 자연스러웠다.

### 🔴 이번에 두 번 밟은 함정
파이썬 heredoc 으로 JS 를 심을 때 `'\n'` 이 **진짜 줄바꿈으로 들어가** 문자열이
두 동강 났다(`join('` + 줄바꿈 + `')`). 두 파일에서 같은 사고. 문법 검사로 잡았다.
→ **JS 안에 이스케이프가 들어가면 heredoc 말고 편집 도구로 직접 고칠 것.**
또 `render();` 뒤에 있던 주석이 치환 뒤 엉뚱한 자리로 밀렸다 - 앵커를 줄 전체로
잡지 않으면 뒤에 붙은 주석이 남는다.

### 시험
`시험_추천노래.py` 15건 통과. 규칙을 파이썬으로 옮겨 적으면 두 벌이 갈라지므로
**node 로 실제 `bgm.js` 를 불러서** 시험한다. 검색어에 따옴표·대시가 섞이면
(곡 제목을 지어낸 흔적) 실패로 본다.

### 산출물
- 커밋 2개: `7e6ff8b`(.spec 를 깃에) · `cefe0d4`(추천 노래) - 둘 다 push
- 폰판 `docs/` 재생성 → 깃허브 페이지 자동 반영
- exe 재빌드 + ZIP 2종 갱신


## 2026-08-24 (10차) - 「릴스 만들기」→「숏폼 만들기」, Shortform Studio 를 뉴보대 안에 동봉

사용자 요구: 바탕화면의 「Shortform Studio」(별도 Electron 앱)를 뉴보대의 한 기능으로
넣고, 머리글 「릴스 만들기」를 「숏폼 만들기」로 바꿔 누르면 그 스튜디오가 열리게. 뉴보대가
큰 틀. 더불어 스튜디오에 **레퍼런스 영상을 분해해 구조를 직접 편집**하는 기능 추가.

### 판단
- WebView2 창 안에 Electron 화면을 붙일 수는 없다. 그래서 「하나의 앱」은 **한 배포 폴더 +
  뉴보대가 자식 프로세스로 띄우고 맥락을 넘기는 것**으로 풀었다. 앱 폴더에
  `shortform\Shortform Studio.exe`(Electron 실행본 통째, 약 350MB)를 동봉하고,
  `POST /api/shortform-launch` 가 `--from-newbodae=<context.json>` 으로 띄운다.
- 스튜디오 쪽은 single instance 라 두 번 눌러도 창이 하나, 맥락(카드 그림·검색어·제목)만
  갈아 끼운다. 편집 창에는 카드가 타임라인에 놓이고, 헌터 창은 검색어로 바로 수집한다.
- 옛 릴스 화면(`reel.html`, 카드 슬라이드 mp4)은 지우지 않고 숏폼 화면 머리글의
  「간단 릴스(카드 슬라이드)」로 남겼다 - 설치 0개 규칙을 지키는 경로는 그대로 있어야 한다.

### 만든 것 (뉴보대)
- `static/nav.js` SCREENS: `릴스 만들기 → 숏폼 만들기(/static/shortform.html)`.
- `static/shortform.html` 신설: 왼쪽에서 out·임시 폴더 카드를 클릭 순서대로 담고, 검색어·
  제목을 넣어 「숏폼 스튜디오 열기」. 앞 화면에서 실어 온 피드 글 첫 줄을 검색어로 미리 넣는다.
- `app.py`: `_studio_exe()`(찾는 순서 = 환경변수 → exe 옆 `shortform\` → `앱\<판>\shortform\`
  → `~/shortform-studio/dist*` 최신) · `GET /api/shortform-status` · `POST /api/shortform-launch`
  (파일은 인스타 올리기와 같은 `_insta_resolve` 검사, 맥락은 `out\_shortform\context.json`,
  DETACHED_PROCESS 로 띄워 서버가 죽어도 스튜디오는 남는다).

### 만든 것 (Shortform Studio, `~/shortform-studio`)
- `src/shortform-analysis.js`: `blueprintFrom`(1편=그 영상 뼈대 / 여러 편=공통 뼈대) ·
  `updateBlueprint`(duration/count/rewrite/redistribute) · `conceptFromBlueprint` · `blueprintOptions`.
- 헌터 화면: 카드마다 「참고로 담기」, 「레퍼런스 분해 · 구조 편집」 구역(담은 칩 → 고른 영상
  분해 → 길이·훅·문체·구조·장면 수 + 장면별 시간·역할·자막·화면·전환·강조 표, 장면
  추가/순서/삭제, 시간 재배분, 문장 새로 짓기, 소재 찾기, 편집 앱으로 보내기).
- `main.js`: `--from-newbodae=` 맥락 읽기 + single instance + `hunter:blueprint*` IPC +
  QA `SHORTFORM_QA_VIEW=hunter` 스크린샷. `verify-editor.js` 가 헌터 id 검사·분해 회귀 시험까지.
- 실행본 `dist-v7\win-unpacked`, 바탕화면 「Shortform Studio.lnk」도 v7 로.

### 검증
- `npm.cmd run check` 통과(편집기 125 · 헌터 43 요소 · 분해 회귀).
- QA 스크린샷(가짜 2편 담기 → 분해)으로 편집 표 렌더 확인.
- 소스 서버(7899)에서 status → launch 를 실제로 눌러 dist-v7 스튜디오가 뜨고 context.json 에
  카드 2장·검색어가 실리며 헌터가 그 검색어로 수집을 시작하는 것까지 확인.

### 🔴 함정
- PyInstaller 는 `앱\뉴보대 카드뉴스 메이커\` 를 **통째로 지우고** 다시 만든다 → `shortform\`
  은 **빌드 뒤에** 다시 복사해 넣어야 한다(빼먹으면 status 가 「없음」, 개발 PC 에서는
  `~/shortform-studio/dist*` 로 조용히 넘어가서 못 알아챈다).
- 공유 ZIP 이 31MB → 약 150MB 로 커진다(Electron 동봉). 카톡 전송 한도에 가깝다.
- E: 원본 드라이브가 이번 세션에 연결돼 있지 않아 **C: 백업 사본만 고쳤다**. E: 를 꽂으면
  `newsfeed\`(app.py·static) 와 `앱\` 을 C: 기준으로 맞춰야 한다.
- bash heredoc 으로 JS/파이썬 패치를 심을 때 `\`·`$` 가 깎였다(정규식 `\$\(` 가 `$(` 로).
  패치 스크립트는 파일로 써서 `node 파일` 로 돌리는 편이 안전했다.

### 덧붙임 - 폰판 머리글
`nav.js` 는 폰판(깃허브 페이지, 서버 0개)도 같이 쓴다. 폰에서 「숏폼 만들기」를 누르면 exe 를
띄울 서버가 없으니, `isPhoneBuild()`(폰shim 의 `NBD_PHONE` 또는 `github.io`/`file:` 주소)일 때는
옛 「릴스 만들기」(reel.html)를 그대로 보인다. 폰판 `docs/` 는 결과가 안 바뀌므로 재생성·푸시
안 함. 첫 ZIP 굽기를 이 수정 때문에 중간에 멈추고 다시 구웠다.

### 산출물 (최종)
- exe 2종 재빌드(본판 PyInstaller → pure 복사·개명) + 각 앱 폴더에 `shortform\`(dist-v7) 동봉, 앱 폴더 405MB.
- 「(앱).zip」·「(pure)(앱).zip」 각 **435항목·169.2MB**(이전 348항목·31MB, 역슬래시 0).
- 바탕화면 「Shortform Studio.lnk」→ `dist-v7`. 「뉴보대 카드뉴스 메이커.lnk」는 경로 그대로(내용만 새 exe).

## 2026-08-25 - E: 외장 다시 연결, C: 와 양방향 병합

- 대조(node_modules·.git 제외, 해시+수정시각): C: 1,584 / E: 1,386 파일. **양쪽 다 바뀐 파일 0**.
  C: 만 새것 36(10차 산출물 전부) + C: 에만 있음 200(`shortform\` 176·bgm.js·shortform.html·build 잔재)
  → E: 로 복사 236개, 해시 재대조 0 불일치. homepage·카드뉴스 메이커는 차이 없음.
- **E: 에만 있던 것 2개** = `앱\본판·pure\갤럭시에서 작업하기.bat`. 삭제한 게 아니라 8/24 exe
  재빌드 때 PyInstaller 가 앱 폴더를 지우면서 **C: 에서 잃어버린 파일**(8/14 일지 "3곳" 중 2곳).
  E: → C: 복원 + ZIP 2종에도 항목 추가(435→436, 전체 재생성 없이 갈아끼움).
- 🔴 재빌드 뒤 앱 폴더에 다시 넣어야 하는 것: `shortform\` 만이 아니라 **`갤럭시에서 작업하기.bat` 도**.
- 🔴 PowerShell 함정: `"$prefix갤럭시..."` 는 한글이 변수명에 붙어 `$prefix갤럭시` 라는 빈 변수가 됨
  → ZIP 항목명이 ` 작업하기.bat` 로 들어갔다가 고침. 한글 앞에 변수는 `${prefix}` 로.
- **복구본 대조(미뤄 둔 것)**: 세 폴더 깃 HEAD 와 대조 - `git fsck` 이상 없음, homepage-work 는 HEAD 와
  완전 일치, 뉴보대 차이는 전부 **10차 미커밋분**(app.py·nav.js·shortform.html·README·설명서·일지).
  제외분 4종(제출DB 1,262B·out 15개·앱 2종·spec) 살아 있음. → 커밋 `e2b065b` + push, `.git` 도 E: 에 미러.
  `카드뉴스 메이커` 는 깃 아님(대조 기준 없음, 어제 해시 일치로 갈음).
- 🔴 E: 는 exFAT 라 git 이 「dubious ownership」으로 거부 - `git -c safe.directory='*'` 로 읽는다.

## 2026-09-01 - 11차: 텔레그램 뉴스 봇 연동 (「오늘의 뉴스」 화면 + 한 번에 발행)

요구: "korea_news 봇이 종합한 뉴스로 뉴보대에서 결과물을 만들어 두고, 확인·수정 후
[발행] 한 번에 인스타 등 SNS 로."

### 통로 - 봇이 JSON 을 남기고 우리는 읽기만
- 봇(`park-hyun-su/korea_news_bot`)은 텔레그램으로 쏘고 **아무것도 남기지 않았다.**
  `save_digest()` 를 넣어 발송 직후 `data/latest.json` + `data/YYYY-MM-DD.json` 을 쓰고,
  워크플로가 레포에 되돌려 커밋하게 했다(`permissions: contents: write`).
- 실측 크기 **하루 14KB**(기사 40 + 해외 겹침 7). 1년 5MB 남짓이라 지울 이유가 없다.
  (지워도 git 이력에는 남으므로 용량이 줄지도 않는다.)
- 받는 쪽 `daily.py` 는 **표준 라이브러리만** 쓴다 - 봇은 aiohttp/bs4 를 쓰지만 우리는
  그 코드를 부르지 않고 결과 파일만 읽는다. 찾는 순서: 환경변수 `NBD_NEWS_DATA` →
  `설정.json` 의 `news_data` → `~/korea_news_bot/data`. 폴더가 git 이면 읽기 전에
  조용히 `git pull`(10분에 한 번, 실패해도 있는 파일로 간다).
- 시험 `시험_오늘의뉴스.py` 11항목(꼴·빈 카테고리·깨진 파일·새 schema·없는 날짜).

### 화면 - `static/daily.html`
- 카테고리 묶음별 목록(조회 순위·댓글 수·매체), 날짜 고르기, 묶음 전체 고르기.
- 넘기는 길 두 가지
  1. **기사 1건 깊게**: `/?url=…&title=…&auto=series` - 기존 `?url=` 규칙 그대로라
     app.js 가 가져오기를 누르고, deck.js 가 본문이 차기를 기다렸다 자동 구성한다.
  2. **여러 꼭지 한 게시물**: localStorage `nb_daily` 에 담고 `/?auto=brief` →
     `DECK.briefFrom()` 이 표지 1 + 기사마다 1장(NEWS 01…)을 세운다. **본문을 안 긁는다** -
     기사 N건을 한 번에 긁으면 느리고 언론사에도 무리다. 살은 사람이 붙인다.
- 실측: 정치·경제 3건 고르기 → 4장 구성 / 단일 기사 → 본문 1619자·사진 2장으로 4장 구성.

### 발행 - `/api/publish` (공식 API 3창구)
- `meta_api.올리기()` 를 그대로 부르는 얇은 창구. 인스타 올리기 화면에 「6. 공식 API 로 발행」
  구역을 붙였다(계정 고르기·창구 체크·점검만/발행). `/api/publish-status` 는 이어진 계정과
  토큰 남은 날만 돌려준다 - **토큰은 화면에 안 준다.**
- 5번(브라우저 CDP, 인스타 한 곳)은 그대로 둔다. 둘은 대체가 아니라 갈래다.
- 🔴 아직 실계정으로 못 봤다. 계정 미연결 상태에서 "이어진 계정 없음" 으로 잠기는 것까지만 확인.

### 🔴 함정
- **bash heredoc 이 역슬래시를 먹는다**(8/24 일지와 같은 것을 또 밟았다). 파이썬 안의
  `'\n'` 이 진짜 줄바꿈이 되어 JS 문자열이 두 줄로 갈라졌다 → `node --check` 로 잡음.
  긴 JS 조각은 **파일로 써서 splice** 하는 쪽이 안전하다.
- exe 재빌드 뒤 앱 폴더에 `shortform\`·`갤럭시에서 작업하기.bat` 다시 넣기(8/25 함정 그대로).
  pure 는 같은 빌드의 `_내부`·exe 를 이름만 바꿔 갈아끼운다.

### 산출물
- 봇: `telegram_news_bot_v3.py`(save_digest) + `news.yml`(커밋 단계) 커밋·푸시, 실행 확인.
- 앱: exe 2종 재빌드 → ZIP 2종 **437항목**(436 → +1 = daily.html)·169.2MB, 역슬래시 0.
- E: 원본 ↔ C: 백업 동기(robocopy /MIR, 811MB 중 바뀐 110MB만), 바탕화면 .lnk 경로 그대로.

## 2026-09-01 - 12차: 노트북 없이 새벽에 굽고 폰에서 발행 (깃허브가 서버)

요구: "앱을 열지 않아도 저장이 되어 확인하고 버튼을 누를 수 있게. 깃허브를 서버로 써서
버튼을 누르면 바로 발행. 발행 이후 기록은 깃허브에만."

### 굽기 - 폰판을 헤드리스 크롬으로 그대로 연다
카드 디자인은 전부 캔버스 JS 라 서버에서 파이썬으로 다시 그리면 두 벌이 되어 갈라진다.
그래서 `docs/`(폰판, 서버 0개)를 임시 정적 서버에 얹고 **사람이 누르는 길**
(`?auto=brief` → `DECK.briefFrom`)을 그대로 밟게 한 뒤 캔버스만 꺼낸다.
- `.github/cards/render.js` (puppeteer-core + 러너의 시스템 크롬, Chromium 내려받지 않음)
- 기사 선별은 **전체 조회 상위 5건**(`korea_news_bot/pick_top.py`) → 표지 1 + 기사 5 + 뒷장 1 = 7장
- JPEG 0.9 로 장당 85~120KB. 로컬과 Actions 산출물이 눈으로 같다(대조 확인).

### 🔴 이번에 밟은 함정 넷
1. **Actions 표현식은 한글 식별자를 못 읽는다.** `inputs.기사` 는 워크플로 파싱 실패(422).
   `env.시늉` 같은 것도 같은 문제 → 표현식에 닿는 이름은 전부 영문으로.
2. **릴리스 자산은 그림 호스팅에 못 쓴다.** 내려받기 주소가 서명 붙은 임시 주소로 302 되고
   `Content-Type: application/octet-stream` + `attachment` 로 내려온다 → `<img>` 에 안 뜨고
   인스타 Graph API 도 못 읽는다. → `cards` **orphan 브랜치를 날마다 force-push**.
   `raw.githubusercontent.com` 이 `image/jpeg` 200 으로 그대로 준다(실측). 어제 것은 닿을 수
   없게 되므로 **저장소가 안 큰다** - docs/ 에 커밋했으면 하루 0.7MB 씩 이력에 영원히 쌓였다.
3. **헤드리스에서 뒷장이 앞 장의 복사본으로 나왔다.** 뒷장은 outro.js 가 그리는데 그 설정·그림은
   장을 한 번 열어야 실린다. `DECK.activate(i)` 로 장을 하나씩 열어 준 뒤 `stageItems()`.
4. **payload 를 윈도 셸에서 만들면 한글이 깨진다**(cp949 → mojibake 카드). 시험할 때는
   `ensure_ascii=True` 로. 실제 경로(봇 → dispatch)는 UTF-8 이라 문제 없다.

### 앱 버그 하나 같이 고침
사진 없는 카드를 내보내면 미리보기용 안내 문구(「사진을 끌어다 놓거나…」)가 **그림에 박혔다.**
자동 구성처럼 사진 없이 만든 장에서 난다. `outCanvas()` 가 내보내기 직전에만 안내를 끄고 다시
그리게 했다(뒷장은 outro 가 그린 것이 얹혀 있으므로 다시 그리지 않는다).

### 승인 - 두 길, 같은 코드
- `docs/오늘.html` (깃허브 페이지) - 카드·문구를 보고 고친 뒤 [발행]. 정적 페이지라 서버가
  없으므로 **그 기기 localStorage 의 PAT** 로 `repository_dispatch` 를 부른다.
  🔴 토큰을 페이지 파일에 박으면 공개라 누구나 쓴다 - 반드시 기기에만 둔다.
- 검토 이슈에 `발행` 댓글 - 토큰이 아무 데도 안 든다. GitHub 로그인 자체가 인증이다.
  `if:` 절에서 라벨·작성자 권한(OWNER/MEMBER/COLLABORATOR)·첫 줄을 다 본다.
- 어느 쪽이든 `publish-cards.yml` → `.github/cards/publish.py` → `meta_api` 창구 함수.
  그림이 이미 공개 주소를 가지므로 meta_api 의 호스팅 단계를 건너뛴다(인스타·스레드는 주소,
  페이스북만 바이트).
- 발행이 끝나면 `cards` 브랜치를 빈 커밋으로 갈아 끼워 그림을 치우고 이슈를 닫는다.
  레포에 남는 것은 `발행기록/YYYY-MM-DD.json`(링크·문구·때) 뿐.

### 아직 안 된 것 (사람 손)
- `korea_news_bot` 시크릿 **CARDS_TOKEN**(new_bo_dea Contents 쓰기) - 없으면 굽기 요청을 건너뛴다.
- `new_bo_dea` 시크릿 **META_CONFIG**(`메타_연결.py` 설정 통째) - 없으면 발행이 거기서 멈춘다.
- 그래서 **실제 SNS 게시는 아직 한 번도 안 해 봤다.** 첫 발행은 `점검만` 부터.

### 산출물
- 봇: `pick_top.py` + 워크플로에 dispatch 단계. 카드: 워크플로 2개·스크립트 3개·`docs/오늘.html`.
- Actions 실행 4번으로 굽기 성공 확인(마지막 33514935279), 검토 이슈 #4, 폰 화면에 카드 7장 표시.
- exe 2종 재빌드 + ZIP 2종(437항목·169.2MB) + E:↔C: 동기화.

## 2026-09-01 - 13차: 홈페이지 「정기 뉴스 메이커」 + 밝은 판/어두운 판

요구: "홈페이지 접속하면 목록이 나와 클릭으로 확인·수정 뒤 업로드. 만든 PNG와 피드 내용이
종류별로 리스트업. API 키가 아직이니 일단 다운로드 폴더에 PNG 가 받아지고 문구는 수정/복사."

### 어느 것이 최신인가부터
바탕화면의 두 입구를 확인했다. `뉴보대 카드뉴스 메이커.lnk` → C: 앱 exe(=`newsfeed/static/`
소스), `뉴보대 공개 주소 (폰 어디서나).html` → 깃허브 페이지로 보내는 QR 한 장짜리 안내.
**최신 코드는 앱 소스 쪽**이고 홈페이지는 그것을 `폰판_만들기.py` 로 구운 것이므로,
홈페이지 갱신 = 소스 고치고 폰판 다시 굽기. (두 벌로 나누지 않는다.)

### 화면 - `docs/오늘.html` 을 「정기 뉴스 메이커」로 다시 씀
- 카드를 **종류별로** 묶어 보여 준다: 표지 / 뉴스 카드(기사 제목 함께) / 뒷장.
- 장마다 **PNG 내려받기**, 위에 **PNG 전부 내려받기**(0.35초 간격 - 브라우저가 연속 저장을 막는다).
  🔴 주소를 `<a download>` 에 그대로 걸면 다른 출처라 새 탭으로 열리고 파일 이름도 안 지켜진다.
  `fetch` → `blob` → `<a download>` 로 받아야 다운로드 폴더에 제 이름으로 떨어진다.
  (raw.githubusercontent 는 `Access-Control-Allow-Origin: *` 이라 fetch 가 된다 - 실측)
- 그림을 누르면 원본 PNG 를 크게 본다. 피드 글은 그 자리에서 고치고 **전체 / 해시태그만 /
  스레드용 500자** 로 나눠 복사한다. 발행 단추는 접어 두고 "API 열쇠가 아직" 이라고 적었다.
- `render.js` 가 이제 **png(내려받기용) + jpg(인스타용)** 두 벌을 굽고 장 종류를 함께 넘긴다.
  하루 png 4.9MB - `cards` 브랜치는 날마다 통째로 갈아 끼우므로 저장소는 안 큰다.

### 밝은 판 / 어두운 판
- `style.css` 의 색을 `:root` 변수로 모으고 `:root[data-theme="light"]` 에 밝은 값을 얹었다.
  머리글의 해·달 단추(nav.js)가 `data-theme` 만 바꾸고 `localStorage.nb_theme` 에 남긴다.
- 🔴 **화면 파일들이 인라인 `style` 로 흰 글씨·어두운 테두리를 박고 있었다.** 변수만 바꿔서는
  머리글 링크가 밝은 판에서 흰 바탕에 흰 글씨가 된다 → `static/*.html` + `nav.js` 의
  158자리를 `var(--ink)`·`var(--line)` 등으로 교체.
- 🔴 색 이름을 붙일 때 **본문 배경(#14161a)을 빠뜨려** 판을 바꿔도 배경이 그대로였다.
  밝기로 훑어 어두운 색 21자리를 더 잡았다. 카드 미리보기는 캔버스라 판과 무관하다(의도).
- 머리글에 **「정기 뉴스 메이커」**(폰판 전용, 파란 강조) 입구를 넣었다. 앱에서는 기존
  「오늘의 뉴스」(서버가 봇 JSON 을 읽는 화면) 그대로.

### 확인
Actions 실행(33519126578) → 카드 7장 png+jpg, 홈페이지에서 종류별 목록·썸네일·판 토글 확인.
🔴 스크린샷이 이미지 로딩보다 빨라 "그림이 안 뜬다"고 오판할 뻔했다 - `naturalWidth` 로 확인할 것.

### 산출물
exe 2종 재빌드 + ZIP 2종(437항목) + E:↔C: 동기화 + 폰판 재생성(파일 44개).

### 덧붙임 (같은 날 저녁) - 입구가 안 보인다는 지적
머리글 칩 하나로는 못 찾는다. 왼쪽 패널 맨 위에 **파란 배너**(`.daily-cta`)를 두었다.
🔴 주소를 nav.js 가 실행 중에만 고치게 했더니, **캐시로 옛 nav.js 가 오면 `daily.html` 로
새서 404** 가 났다(실측). `폰판_만들기.py` 의 `relativize()` 에서 **구울 때 `오늘.html` 로
박아 두고**, nav.js 는 보조로 남겼다. 화면 코드가 판을 타는 곳은 빌드에서 정하는 게 안전하다.

### 홈페이지 ↔ 앱 기능 차이 (2026-09-01 기준, 의도된 것)
| 화면 | 앱 | 홈페이지 | 왜 |
|---|---|---|---|
| 앞장·뒷장·워터마크·릴스·참고 사이트 | O | O | 같은 최신 코드(폰판으로 구움) |
| 오늘의 뉴스 / 정기 뉴스 메이커 | O(서버판) | O(오늘.html) | 같은 일, 다른 재료 |
| 피드 글 | O | X | 캡션 생성이 `feed.py` 512줄 - JS 이식하면 가능 |
| 주제 찾기 | O | X | RSS 수집이 CORS 에 막힘 - 대리인 경유하면 가능 |
| 인스타 올리기 | O | X | 크롬 조종(CDP) - 서버 없으면 불가 |
| 폴더 정리 | O | X | PC 의 out 폴더 - 불가 |
| 숏폼 스튜디오 | O | X | exe 실행 - 불가 |

## 2026-09-02 - 14차: 카드에 기사 사진·요약, 화면에 카드별 글

지적 둘 - "png 가 검은 화면으로 보인다", "각각을 눌렀을 때 피드 내용을 알 수 없다".
브리핑을 세울 때 **본문을 안 긁도록** 해 둔 것이 원인이었다(빠르지만 할 말이 없는 카드).

### 손질 단계를 앞에 넣음 - `.github/cards/prep.py`
기사마다 한 번씩 본문을 읽어 ①`og:image` 를 **파일로 내려받고** ②본문 두 문장 요약
③언론사 이름을 기사.json 에 채운다. 5건이면 요청 5번, 실패한 기사만 사진·요약 없이 간다.
- 🔴 사진을 **주소 그대로** 캔버스에 얹으면 다른 출처라 캔버스가 오염돼 `toDataURL()` 이
  막힌다 → 받아 두고 `render.js` 가 `/사진/` 으로 **같은 출처**에서 얹는다.
- 🔴 `summarizer.summarize()` 는 문자열이 아니라 **후보 목록**을 준다. 첫째만 쓴다
  (모르고 목록을 그대로 넣어 "요약 4자"가 나왔다).

### 카드 - `deck.js briefFrom`
표지와 기사 장에 사진을 깔고, 본문 자리에 요약을 넣는다(요약이 없으면 언론사 이름).
- 🔴 `const list = [{ ... }]` 한 줄이 **`autoCompose()` 에도 똑같이** 있어서
  치환이 엉뚱한 함수에 들어갔다. 되돌리고 briefFrom 안에 다시 넣었다.
  같은 모양의 줄이 여럿이면 앵커를 넓게 잡을 것.

### 화면 - 카드를 누르면 그 카드의 글
`오늘.json` 의 카드마다 `feed`(제목+요약+출처)를 담고, `오늘.html` 은 카드를 누르면
그림과 글을 함께 보여 준다. 「이 카드 글 복사」·「PNG 내려받기」가 그 자리에 있다.
- 🔴 공개 자리에 **jpg 만 올리고 있었다** → 화면의 PNG 주소가 404. png 도 같이 올린다.

### 산출물
Actions 실행 확인(사진 5/5·요약 5/5, 카드 139~223KB), 홈페이지에서 카드별 글 확인.
exe 2종 재빌드 + ZIP 2종(437항목) + E:↔C: 동기화.

### 남은 것
앱의 「오늘의 뉴스」 브리핑은 아직 사진이 없다(앱은 제목만 넘긴다). 앱에서도 사진을 넣으려면
고른 기사마다 본문을 긁어야 해서, 누를 때 5건을 한꺼번에 받는 부담을 어떻게 둘지 정해야 한다.

## 2026-09-02 - 15차: 앱 브리핑에도 기사 사진·요약 (14차가 남긴 것)

14차 일지의 마지막 줄이 숙제였다. 새벽 자동 굽기(Actions)는 기사마다 본문을 한 번 읽어
사진·요약을 챙기게 고쳤는데, **앱의 「오늘의 뉴스」는 제목만 넘기고 있었다.** 같은 화면인데
폰으로 본 카드에는 사진이 있고 앱에서 만든 카드는 검은 화면인 상태였다.

### 알맹이를 한 벌로 - `newsfeed/prepare.py`
`.github/cards/prep.py` 안에 있던 손질 코드(og:image 내려받기 · 두 문장 요약 · 언론사)를
`newsfeed/prepare.py` 로 옮기고, 자동 굽기 쪽은 파일을 읽고 쓰기만 하는 **얇은 CLI** 로
남겼다. 두 벌로 두면 한쪽만 고쳐진다 - 실제로 14차에 앱 쪽이 그대로 남았던 이유가 그것이다.
- `한건(it, 사진폴더, 이름)` · `손질(items, …, 이름짓기=)` · `묵은사진지우기(폴더, 날수)`
- `이름짓기` 를 밖에서 넣게 둔 이유: 자동 굽기는 한 판에 01·02… 지만, 앱은 부를 때마다
  1번부터 시작하므로 **주소 해시**로 이름을 지어야 서로 안 덮어쓴다.

### 앱 - `/api/daily-prep` + 같은 출처 사진 자리
- `POST /api/daily-prep` : 고른 기사를 손질해 돌려준다. 사진은 원본 주소가 아니라
  **`/기사사진/…`**(= `out\_임시_기사사진\`) 우리 서버 경로로 준다. 다른 출처 그림을 캔버스에
  얹으면 오염돼 `toDataURL()` 이 막힌다 - 굽기 쪽이 겪은 것과 같은 함정이다.
- 「오늘의 뉴스」 아래 띠에 **사진·요약 챙기기** 체크상자(기본 켜짐, `nb_daily_prep` 에 기억).
  켜져 있으면 브리핑으로 넘기기 전에 기사를 **한 건씩** 손질하며 `기사 손질 중 2/2 — …` 로
  몇 건째인지 보여 준다. 한 건씩 부르는 이유가 그 진행 표시다(한꺼번에 부르면 몇 초간 멈춘 화면).
  실패한 기사는 사진·요약 없이 간다 - 카드는 어떻게든 나와야 한다.
- 받아 둔 사진은 3일 지나면 지운다(앱이 계속 쓰는 폴더라 쌓이면 안 된다).

### 확인 (헤드리스 크롬으로 사람 길 그대로)
- `시험_기사손질.py` **10항목** - 후보 목록 첫째만 쓰기 / 1x1 그림 거르고 다음 후보로 /
  그림 아닌 응답 거르기 / 본문 실패는 그 기사만 / 이름짓기 갈아 끼우기 / 묵은 사진만 지우기.
- 실제 기사(머니투데이 성수품)로 `/api/daily-prep` → 사진 94KB · 요약 78자 · 언론사 채움.
- 화면을 눌러 본 결과: 기사 2건 체크 → 「브리핑 만들기」 → `기사 손질 중 2/2` → 3장
  (표지+기사 2), 세 장 모두 `/기사사진/…` 이 깔리고 **`toDataURL()` 2.4MB 로 통과**(오염 없음).
- 구운 exe 로도 같은 창구를 확인(사진 HTTP 200 · image/jpeg).

### 🔴 함정
- **bash heredoc 이 또 역슬래시를 먹었다**(11차와 같은 것). 이번엔 JS 안의
  `'C:\Program Files\…'` 이 `C:Program FilesGoogle…` 이 되어 "크롬 없음" 으로 나왔다.
  역슬래시가 든 조각은 파이썬으로 쓰거나 `String.raw` 로 감쌀 것.
- **PowerShell `Copy-Item -Recurse` 는 대상 폴더가 이미 있으면 그 안에 넣는다.**
  pure 판 `shortform\` 안에 백업 폴더가 통째로 들어가 ZIP 이 523항목·323MB 가 됐다.
  ZIP 은 구운 뒤 반드시 **항목 수를 두 판 비교**할 것(437 = 437).

### 산출물
exe 2종 재빌드(+ `shortform\`·`갤럭시에서 작업하기.bat` 다시 넣기) · ZIP 2종 **각 437항목**
· 번들 static 해시 = 소스 해시 확인 · E: 동기화.

## 2026-09-02 - 16차: 지금 뉴스로 다시 만들기 · 글보기 개조 · 네 글투 · 같은 발언 인용

요구 다섯 가지. ①정기 뉴스 메이커의 새로고침이 **지금 시각** 뉴스를 다시 불러오고 그것으로
카드를 다시 만들 것 ②글보기에서 **사진 수정**과 **글 여러 개** ③홈페이지에서 카드 편집 판이
릴스·뒷장·피드 글 단추를 **가리는 것** 고치기 ④피드 글 **네 가지 글투**를 정기 뉴스 메이커에도
⑤기사 링크를 넣으면 **인용한 쌍따옴표 문장이 같은 기사 3건 이상**을 요약해 같은 맥락으로 쓸 것.

### ③ 가리는 문제 - 원인은 가운데 정렬의 위쪽 넘침
홈페이지를 1440x900 으로 열어 머리글 단추마다 `elementFromPoint` 를 찍어 봤다.
**「뒷장 만들기·정기 뉴스 메이커·참고 사이트·릴스 만들기·판 토글」 다섯 개가 `#deckPages` 에
덮여** 있었다(폰 390 은 멀쩡). `.stage` 는 `justify-content:center` 인데 장 목록 띠가 붙으면서
안쪽 내용이 칸보다 커졌고, 가운데 정렬은 넘친 만큼을 **위아래 양쪽으로** 흘린다.
- `.stage.has-deck{justify-content:flex-start}` - 위에서부터 쌓아 위로는 넘치지 않게.
- `.stage.has-deck .canvas-box{flex:1 1 0;min-height:0;height:auto}` - 캔버스가 남는 높이만
  차지한다. 손으로 빼던 `calc(100% - 34px - 104px)` 는 띠 높이가 바뀌면 또 틀어진다.
- `.top{position:relative;z-index:60}` - 빗장 하나 더. 머리글은 무슨 일이 있어도 위에 있어야 한다.
- 폰은 미리보기를 화면 위에 붙여 두므로 `flex:none` 으로 높이를 못 박았다.
- 확인: 390/1280/1440/1600 네 크기에서 **가려진 단추 0개**(고치기 전 1440 에서 5개).

### ⑤ 같은 발언을 인용한 기사 - `related.quotes` · `find_quoted`
낱말 검색은 '주제가 비슷한' 기사까지 끌어오지만, **같은 발언을 글자 그대로 옮긴 기사**는
같은 사안이 거의 확실하다. 그래서 본문의 쌍따옴표를 뽑아 정확 구절로 검색한다.
- `related.quotes(body, n)` : 홑따옴표는 강조에도 쓰여 뺀다. 기자가 끼워 넣은 **닫히지 않은
  괄호**(예: "…생각에서 (민주당TV")는 매체마다 달라 잘라 낸다 - 안 자르면 0건이 된다.
- `find_quoted(발언 또는 발언들)` : `need`(기본 3)를 채울 때까지만 찾는다.
  🔴 **가장 긴 발언이 가장 잘 걸리는 것이 아니다**(실측: 1번 발언 1건 / 3번 발언 8건).
  그래서 발언 여러 개를 **가로로** 훑는다 - 첫 발언을 짧게 자르며 매달리는 것보다 낫다.
  토막을 줄일 때는 **어절 경계**로 자른다(가운데를 자르면 구절 검색이 0건).
- `find()` 가 인용으로 걸린 것을 **앞자리**에 세우고 `by_quote` 로 표시한다. 피드 글 화면은
  기본 선택도 인용 건수만큼 잡고, 목록에 「같은 발언 인용」 배지를 단다.
- `feed.quote_block()` + `_quote_section()` : 네 글투 **모두**에 인용 절이 들어간다.
  3건을 못 채우면 "3건을 채우지 못했습니다" 라고 적는다 - 부풀리지 않는다.
- 실측(민주당TV 기사): 인용 7건, 캡션에 6개 매체가 함께 들어감.

### ①②④ 정기 뉴스 메이커(docs/오늘.html) 개조
- **새로고침 = 지금 뉴스**. 구운 카드를 다시 읽고, 이어서 네이버 뉴스 랭킹을 **지금** 긁어
  30건을 보여 준다. 고르고 「고른 기사로 카드 만들기」를 누르면 기사마다 사진(og:image)·
  두 문장 요약을 챙겨 `nb_daily` 에 담고 「앞장 만들기」로 넘긴다. 카드는 그쪽 캔버스가 그린다
  (여기서 다시 그리면 두 벌이 되어 갈라진다).
- **글보기**가 두 쪽이 됐다. 「글」쪽은 네 글투 + 「카드에 붙은 글」 탭이고 그 자리에서 고쳐
  복사한다. 「본문 가져와 더 채우기」를 누르면 기사 본문을 읽어 와 문장이 늘어난다.
  「사진 고치기」쪽은 내 사진(1080px 로 줄여 data URL) · 기사 사진 다시 가져오기 · 주소 넣기 →
  「이 사진으로 카드 다시 만들기」.
- **네 글투**는 `폰판_소스/feedstyles.js`(feed.py 의 옮김). 전체 캡션도 글투를 바꿀 수 있고
  「새벽 초안」 탭으로 워크플로가 쓴 원래 글로 돌아간다.
- 인용 묶음은 화면에 상자로 보여 준다.

### 인용 검색은 **굽는 쪽**에서 - 폰은 못 한다
r.jina.ai 는 구글 뉴스를 **403** 으로 막고, 네이버 검색 화면은 짜임이 자주 바뀐다(실측).
그래서 `prep.py`(새벽 Actions)가 기사마다 발언을 뽑고 같은 발언을 실은 보도를 찾아
`오늘.json` 에 `quoted` 로 넣어 두고, 화면은 **보여 주기만** 한다.
`caption.py` 도 그 묶음과 기사별 요약을 초안에 넣는다.

### 🔴 함정
- **bash heredoc 이 역슬래시를 먹는다** - 이번엔 JS 안의 `'C:\Program Files\…'` 가 뭉개져
  "크롬 없음". 15차와 같은 것을 또 밟았다. **역슬래시가 든 파일은 Write 도구로 쓸 것.**
- 새벽에 구운 기사의 `photo` 는 **굽는 기계 안에서만 있던 파일 이름**(01.jpg)이다. 공개 자리에
  없으므로 화면에 그대로 걸면 404 다 → `쓸사진()` 으로 절대 주소·data 만 쓴다.
- `deck.js briefFrom` 이 사진을 얹을 때 **남의 주소면 대리인을 거치게** 했다. 안 그러면 폰에서
  캔버스가 오염돼 저장이 통째로 막힌다. 우리 서버 경로(`/기사사진/`·`/사진/`)는 그대로 둔다.

### 확인
- 시험: `시험_인용찾기.py` 16항목 · `시험_글투.py` 15항목 신설, 기존 18개 파일 전부 통과.
- 홈페이지 판을 실제 브라우저로: 지금 뉴스 30건 → 2건 골라 카드 4장(표지+뉴스 2+뒷장),
  사진은 언론사 사진이 실렸고 `toDataURL()` 2.8MB 로 통과(오염 없음).
- 글보기: 네 글투가 각각 다른 글(327/315/287/489자) + 「카드에 붙은 글」.
- `prep.py` 실기사 2건: 사진 2/2 · 요약 2/2 · 인용 묶음 2건(8건·6건).

### 산출물
exe 2종 재빌드 · ZIP 2종 각 437항목 · 폰판(docs) 재생성 · E: 동기화.

## 2026-09-02 - 17차: 「피드 글 만들기」를 홈페이지에도 되살림 + 채널(인스타·스레드·X…)

"기존에 있던 기능이 사라졌다 - 기사를 넣으면 주소를 유지한 채 피드 내용까지 만들고,
기본 모드에서 뒷장 만들기처럼 볼 수 있었다"는 지적. 확인해 보니 **앱에는 그대로 있고
홈페이지(폰판)에만 없었다** - 캡션 생성기(feed.py 512줄)가 서버에만 있어서 폰판을 구울 때
화면을 통째로 빼고 머리글 링크도 감춰 왔다.

### 화면을 새로 만들지 않았다
`feed.html` 을 **앱과 같은 파일 그대로** 폰판에 담고, 서버가 하던 계산만 폰shim 이 맡는다.
(화면을 두 벌로 나누면 반드시 갈라진다 - 이 프로젝트에서 이미 겪었다)
- `폰shim.js` 에 `/api/feed`·`/api/titles`·`/api/fetch-many` 추가. 계산은 `feedstyles.js`.
- `/api/related`(유사 기사 **검색**)만 안 된다고 분명히 답한다 - 구글이 대리인을 막는다.
  대신 「링크 직접 넣기」로 넣은 기사는 본문까지 가져온다.
- 🔴 폰판은 **넣은 기사 본문에서 기준 기사의 발언을 찾아** 인용 묶음을 만든다.
  검색은 못 해도 "같은 발언을 실은 보도"는 이렇게 확인할 수 있다.
- 「out 폴더에 저장」은 폰판에서 실제로는 내려받기라 이름을 「글 내려받기」로 바꿔 준다.

### 글투 4 → 6, 그리고 채널 5
- 글투에 **질문 던지기 · 한 줄 + 태그** 를 더했다(인스타 올리기 화면의 캡션 후보에만
  있던 결을 글투로 올림). 앱·폰 양쪽 모두 6종.
- **어디에 올리나**(채널) 를 따로 고른다: 인스타 2,200자 / 스레드 500자 / X 280자 /
  페이스북 · 블로그(길이 자유). 채널은 **문장을 새로 쓰지 않는다** - 길이에 맞춰
  뒤에서부터 줄을 덜어 내고 해시태그 개수만 줄이며, **출처 줄은 끝까지 남긴다.**
  🔴 내용이 다 잘려 절 제목(📌·🗣)만 남으면 그 제목도 같이 뗀다.

### 🔴 함정
- 본문 긁기가 `p` 를 전부 모으고 있어 네이버 안내문("머니투데이 언론사 구독되었습니다",
  "보러가기")이 캡션 첫 줄로 나갔다 → 기사 본문 칸을 먼저 보고, 없을 때만 p 를 훑는다.
- 그 걸러내기를 **본문 칸 통째**에도 적용했더니 저작권 문구 한 줄 때문에 경향신문 기사가
  0자가 됐다(시험 5항목 실패). 거르기는 p 를 훑는 길에서만 한다.
- Git Bash 는 `/static/feed.html` 같은 인자를 윈도 경로로 바꿔 버린다 → 앞 슬래시를 떼고 넘길 것.

### 확인
`시험_글투.py` 21항목(채널 6항목 추가)·`시험_폰_기사가져오기.py` 37항목 전부 통과.
홈페이지 실제 브라우저: 머리글 「피드 글」 보임·안 가려짐, 글투 6종이 서로 다른 글(6/6),
채널 5종(인스타 617 / 스레드 347 / X 111자, 전부 출처 남음). 앱 창구도 같은 결과.
산출물: exe 2종·ZIP 2종 각 437항목·폰판 재생성(화면 6개)·E: 동기.

## 2026-09-02 - 18차: 홈페이지 기사 요약을 「3문장짜리 문단 2개」로

"기사 요약이 너무 압축돼 있다 - 3문장짜리 문단 2개 정도였으면 좋겠다"는 요구.
지금까지 요약은 한 벌뿐이었고 **카드 그림에 얹을 두 문장**(limit 95)이었다. 카드 칸은
좁아서 그 길이가 맞지만, 홈페이지에서 **읽는** 자리에서는 무슨 일인지 알 수 없었다.

### 요약을 두 벌로
`prepare.긴요약(본문, 제목, 문단=2, 문장=3)` 을 새로 만들어 `summary_long` 에 담는다.
카드용 `summary` 는 **그대로 둔다** - 두 자리는 필요한 길이가 다르다.
- 문장은 새로 쓰지 않고 본문에서 고른다(지어내지 않는 규칙 그대로).
- 고른 뒤 **기사에 나온 차례대로** 되돌린다. 점수 순서로 두면 말이 이어지지 않는다.
- 🔴 점수 위에서부터 여섯 문장을 자르면 **같은 말이 두 번** 들어간다(실측: "역대 최대
  할인 지원 예산 590억원" 이 두 문단에 다 나왔다). 후보를 넉넉히 받아 낱말이 6할 넘게
  겹치는 문장을 걸러 낸 뒤 여섯 개만 쓴다.

### 쓰는 자리
- `daily-cards.yml` 이 카드마다 붙이는 글(`feed`)에 긴 요약을 쓴다 - 홈페이지에서
  카드를 눌렀을 때 보이는 그 글이다.
- `feedstyles.js` 의 `lines()` 차례를 **본문 → 긴 요약 → 짧은 요약**으로. 본문을 아직
  안 가져온 카드도 이제 여섯 문장으로 글이 선다(실측 213자 → 356자).
- 카드 **그림**과 여러 기사를 한 줄씩 늘어놓는 묶음 캡션은 짧은 요약 그대로다
  (5건 × 6문장이면 인스타 상한 2,200자를 넘긴다).

### 확인
`시험_기사손질.py` 15항목(긴요약 5항목 추가)·`시험_글투.py` 21항목 통과. 실기사 두 건으로
문단 2개·문장 3개, 겹치는 문장 없음 확인. exe·ZIP·폰판·E: 동기.

## 2026-09-03 - 19차: 요약 두 벌의 자리 정리 + 정기 뉴스 메이커 「그 자리에서 고치기」

18차를 잘못 알아들었다. **카드 그림에 얹는 것은 두 줄짜리 짧은 요약이 맞고**, 3문장짜리
문단 2개는 **피드 글** 이야기였다. 카드 그림 쪽은 원래부터 `summary`(95자)를 쓰고 있어
손댄 곳이 없었지만, 넘길 때 긴 요약이 섞이지 않도록 못을 하나 박았다.
- `카드만들기()` 가 넘기는 `summary` 는 **짧은 것**이다. 짧은 것이 없으면 긴 요약의
  **첫 문장만** 잘라 쓴다(긴 요약을 통째로 넣으면 카드가 글로 덮인다).
- 묶음 캡션(여러 기사)은 기사마다 한 줄이던 것을 **두 문장**으로 늘렸다. 5건 × 두 문장이면
  인스타 2,200자 안에 들어온다.

### 그 자리에서 고치기
"각 뉴스를 클릭하거나 새로고침을 눌렀을 때 그 자리에서 수정" 요구. 복잡하지 않았다.
- **카드를 눌렀을 때**: 모달 맨 위에 제목 칸과 요약 칸이 생겼다. 고치고 「고친 대로 저장」을
  누르면 카드 목록 제목·카드별 글·전체 피드 글이 그 자리에서 다시 그려진다.
  「원래대로」로 되돌린다. 「이 사진으로 카드 다시 만들기」는 저장을 안 눌렀어도 칸에 적은
  제목·요약을 그대로 가져간다.
- **새로고침(지금 뉴스)**: 줄마다 ✎ 를 눌러 제목을 그 자리에서 고친다. 언론사 제목이
  그대로 카드에 올라가면 안 되기 때문이다.
- 🔴 고친 것은 `오늘.json` 에 못 담는다(새벽마다 워크플로가 다시 쓴다). **이 기기의
  localStorage `nb_today_edits`** 에 기사 주소를 열쇠로 남기고, 화면을 그릴 때 덮어씌운다.
- 🔴 Enter 로 편집을 끝낸 뒤 blur 가 한 번 더 와서 이미 뗀 칸을 또 바꾸려다 NotFoundError
  가 났다(실측) - 한 번만 돌게 잠갔다.

### 확인
브라우저로: 고치기 → 저장 → 모달 제목·카드 목록·카드별 글·전체 글 반영, **새로고침 뒤에도
남음**, 「원래대로」 뒤 사라짐, 지금 뉴스 ✎ 로 제목 교체까지 오류 0. 시험 21항목 통과.
